import {expect,test} from "@playwright/test";
test.skip(process.env.PLAYWRIGHT_E2E_AUTH_BYPASS!=="1","Requires local fixture");
test("P33 Goals UI available on both surfaces",async({page},info)=>{
 test.skip(info.project.name!=="chromium","Explicit viewport matrix");test.setTimeout(180000);const errors:string[]=[];page.on("pageerror",e=>errors.push(e.stack??e.message));
 for(const surface of ["workbench","companion"]){await page.goto(`/surface/${surface}?next=${encodeURIComponent("/goals")}`);await expect(page.locator("[data-goals-ui]")).toBeVisible({timeout:60000});for(const [width,height]of[[1440,900],[1280,800],[390,844],[360,800],[1023,800],[1024,800]]){await page.setViewportSize({width,height});await page.evaluate(()=>scrollTo(0,0));await expect(page.getByRole("heading",{level:1,name:"Goals",exact:true})).toHaveCount(1);await expect(page.getByRole("button",{name:"Edit season plan",exact:true})).toBeVisible();await page.screenshot({path:info.outputPath(`P33-${surface}-${width}.png`)});expect(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1)).toBe(false);}await page.setViewportSize({width:390,height:844});await page.getByRole("button",{name:"Edit season plan",exact:true}).click();await expect(page.getByLabel("Measured sessions per week")).toBeVisible();await page.getByRole("button",{name:"Cancel",exact:true}).click();const evidence=page.getByRole("button",{name:"Evidence",exact:true}).first();if(await evidence.count()){await evidence.click();await expect(page.getByText("Measurement date",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Close evidence",exact:true}).click();}}
 expect(errors).toEqual([]);
});
