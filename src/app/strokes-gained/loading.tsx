import { GolfRouteLoading } from "@/components/golf-loading";

export default function StrokesGainedLoading() {
  return (
    <GolfRouteLoading
      title="Finding the leak"
      subtitle="Loading category leaks, hole impact, and the practice job behind the numbers."
      variant="strokes"
    />
  );
}
