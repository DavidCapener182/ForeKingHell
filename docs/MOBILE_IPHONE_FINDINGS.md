# iPhone recovery findings — 5 September 2026

Observed on an iPhone 17 Pro Max through iPhone Mirroring. The handset was on train Wi-Fi; Safari failed to establish a secure connection to both the production alias and the exact released deployment URL. These failures prevent a full live-route sign-off and are not established as an application/server defect.

| Finding                                                            | Change and verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Saved golf feels like a dead end without the five destinations     | Offline recovery now retains Today, Practice, Play, Progress and Bag. Tabs select saved content locally; unavailable Progress explains that it needs a connection. Tabs hide for immersive practice/scoring. Local 320px browser: five 56px-high targets, no overflow, section deep link and Back restore selection.                                                                                                                                                                                                          |
| Reconnect returns to the same page without feedback                | An anonymous, uncached eight-second probe verifies the app's exact marker. Failed, timed-out or captive-portal responses show a useful status and preserve the saved view. Successful retry performs a full navigation into the selected companion destination, replacing the recovered document. Tab changes cancel stale retry navigation. Unit tests cover real marker, HTML substitution, HTTP error, TLS failure and timeout. Local click-through verified both failure retention and successful navigation to Progress. |
| Empty-cache failure shows a small legacy plain-text error          | The service worker has a self-contained ForeKingHell HTML recovery screen, with readable system type, light/dark treatment, safe-area padding and retry/saved-golf actions. It needs no scripts, fonts or images. Runtime test verifies the uncached network-failure response.                                                                                                                                                                                                                                                |
| Standalone top headings appeared blurred under the status-bar area | Offline content now starts a full spacing unit below the safe-area inset, including immersive views. Narrow local browser inspected; physical iPhone confirmation on the new deployment remains pending.                                                                                                                                                                                                                                                                                                                      |
| Existing home icon/name show LMWT                                  | Investigation confirms this is the current shared branding configuration in `src/lib/brand.ts`, not proof that the icon itself failed to update. No shortcut or saved storage was removed. The handset's old offline composition does differ from the current source, so the new worker cache generation v11 retires v10 and earlier shells. A branding change is not silently applied to desktop.                                                                                                                            |

## Data preservation

The fix does not remove account-owned local storage, create golf measurements, complete activity blocks or change calculations. On the handset, saved Quick Bag Carry/Total and club evidence disclosure worked. Range Mode resumed, changed blocks, opened notes/options and paused back to Saved golf; the first block was restored, with 0/7 completed and 14 balls remaining.

## Release verification

The fix passed 525 Vitest files / 2,481 tests, full ESLint, standalone TypeScript, production build, formatting and all unchanged route budgets. PR and deployment history provide release status. The unrelated local nightly workflow remains excluded.

The full live five-tab walkthrough, current imagery/3D scene, successful account mutations, physical keyboard/safe-area acceptance and handset heat/battery observations remain unverified by this pass. Local browser validation is reported separately from actual iPhone observations.

## Review corrections and subsequent handset observations

- Recovered documents now derive the selected destination from the failed route or installed shortcut, retaining its query/deep-link state. External shortcut targets are rejected.
- Every saved-activity navigation and Back invalidates any pending reconnect, with a tested cancellation generation preventing late responses from interrupting practice/scoring. A saved bag counts as Play content, avoiding a contradictory empty state.
- After connectivity returned, the actual iPhone loaded Today, Practice with the corrected photograph, Range Mode and its options/Pause return, and Play with Aintree's aerial. The original 3D terrain, dispersion and planned path rendered, and Hole 2 selection worked. These observations used the preceding deployed release while this recovery fix was undergoing CI; they do not certify the new offline shell on the handset or successful measured-data mutations.

## Production recovery follow-up

- At 21:19 BST the actual iPhone loaded the current canonical production offline shell: clear connection status, readable top spacing and all five tabs. Practice selected its own heading and active state. The canonical origin had no saved entries; the preceding immutable deployment origin retained its saved practice and bag. No browser storage was removed. Standalone installed-app upgrade and the remaining tabs still need handset verification.
- A subsequent password sign-in attempt reached the shared page error screen. Retry did not recover it. Vercel logs for 20:18–20:22:30 UTC show the successful connection probe, login GET and subsequent unauthenticated Today redirect, but no login POST or server error. A dropped transport request is plausible; the exact handset exception was not captured, so the original trigger remains unconfirmed.
- Password and email-link forms now recover the known browser fetch TypeErrors within their action state. The form shows an honest unconfirmed-request message and permits a deliberate retry. Email-link feedback asks the user to check their inbox first because a lost response does not prove the email was not sent. Redirect exceptions, validation responses and unrelated application errors retain their existing behavior. Regression tests simulate Safari, Chromium and Firefox transport failures and verify a subsequent retry, validation and redirect propagation.
- The shared route error screen keeps Next's Retry and adds a full page reload. Today/dashboard and saved-golf escape links perform document navigation so recovery does not depend on a damaged client router. This fixes the observed lack of a second recovery path; it does not establish the cause of the original sign-in failure.
- Follow-up device verification currently requires local Touch ID/Mac authentication for iPhone Mirroring. Current production runtime logs also report a missing `SUPABASE_SERVICE_ROLE_KEY`; its effect on features requiring that credential is not assessed here, and no credentials were copied or changed.

### Sign-in review and continued checks

- Review identified that wrapping a Server Function before hydration removes native form POST metadata. The server render and hydration pass now use the original Server Functions; transport recovery is enabled only after hydration. Actual local HTML retained native POST metadata for all three forms. Browser regression tests passed for a dropped hydrated request followed by retry, and submission with JavaScript disabled.
- The handset subsequently signed in successfully on the canonical production origin. Bag rendered 8 trusted clubs and the 3D carry comparison. Progress rendered real data; Carry/Control switched correctly and the score explanation expanded. These checks used production before the sign-in recovery release.
- **Resolved observation:** subsequent uninterrupted physical iPhone checks passed Bag 3D-to-2D switching and Done. The earlier interrupted closures do not establish a reproducible application defect. Both isolated WebKit mouse/touch checks also passed; no speculative drawer change was made.

## Final findings reconciliation

- **Offline navigation and retry:** released in PR #51. On the installed shortcut's stable domain, `lm-world-tour.vercel.app`, actual signal loss retained Practice selection. All five offline destinations worked. The freshly saved 22:23 Quick Bag reopened with current measurements, and a failed reconnect preserved the view with a clear error. Safari verification does not certify the separate standalone PWA upgrade.
- **Sign-in recovery:** released in PR #52 (`ca8d631c`). Production native POST metadata is present on both stable domains; hydrated transport recovery and JavaScript-disabled submission passed regression tests.
- **Incorrect Driver compact title:** the shell previously collapsed the right heading but displayed the broad route label, “Bag map”. Detail screens now use the visible heading, scoped to the current location, and react to streamed text changes. Root destination labels and explicit Settings/practice-plan labels remain stable. WebKit and Chromium cover collapse, hidden desktop headings, navigation, text updates and heading removal.
- **Supabase configuration:** the existing service-role key was present in the correct Supabase project. Vercel had neither a project variable named `SUPABASE_SERVICE_ROLE_KEY` nor a linked shared variable. After explicit user authorization, the existing key was saved as a Production-only Secret. No key was rotated, revoked or committed. No evidence establishes when or why this configuration went missing; redeployment verification is recorded in the working log.
- **Installed name/icon:** LMWT remains the intentional shared configuration in `src/lib/brand.ts`; the bookmark points to the stable production alias. No shortcut or account-owned storage was removed.
- **Top spacing:** the current offline shell was readable on the physical phone in Safari. Independent standalone upgrade/legibility acceptance remains pending.
- **Connection and Mirroring interruptions:** intermittent train connectivity and Mirroring authentication/concurrent interaction are test limitations, not confirmed application defects. No TLS bypass or network-settings change was applied.

The remaining full-route handset walkthrough, standalone upgrade and device heat/battery checks are coverage gaps, not completed acceptance. The original 3D Course Twin, golf calculations and account records are preserved.
