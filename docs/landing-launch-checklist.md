# LM World Tour public-site checklist

Checked locally on 14 September 2026. The changes are in the shared checkout and have not been committed, pushed or deployed by this task. Vercel Web Analytics was enabled in the existing `lm-world-tour` project through the Vercel UI using the option included in the current plan; Web Analytics Plus was not selected. Its dashboard currently shows no visitor data and asks for deployment of the integration.

| Requested item                 | Implementation and evidence                                                                                                                                                                                                                                                                                                  |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Custom 404                     | Branded recovery page with home and sign-in links. Unknown public routes return HTTP 404; existing account routes still require sign-in. Browser and proxy tests cover both.                                                                                                                                                 |
| Meta title on every page       | Root metadata supplies the site title to every route. Public home, privacy, terms, cookies, sign-in and confirmation have descriptive titles. Account pages inherit the brand default unless they already supply their own title.                                                                                            |
| Meta description on every page | Root description is inherited throughout the app, with specific descriptions for the public routes above. Signed-in pages remain excluded from indexing.                                                                                                                                                                     |
| CTA above the fold             | Join button in the desktop and mobile header, plus the hero actions. Mobile header CTA stays reachable while the cookie notice is open.                                                                                                                                                                                      |
| Favicon                        | Existing favicon, small icons and Apple touch icon retained. Browser check confirms favicon response and icon metadata.                                                                                                                                                                                                      |
| robots.txt                     | Existing generated route now passes the public auth boundary and points to the new domain's sitemap. Account and token/share URLs remain excluded.                                                                                                                                                                           |
| sitemap.xml                    | Public home, privacy, terms and cookies listed under `https://lmworldtour.app`. Sign-in, confirmation and account routes omitted.                                                                                                                                                                                            |
| Open Graph image               | Existing generated 1200 × 630 share image now accessible without sign-in. Image response and page metadata verified. Next development uses a localhost image origin; production metadata base is the purchased domain.                                                                                                       |
| Alt text on every image        | TypeScript JSX audit found 48 `img`/`Image` elements and no missing `alt` attributes. Decorative illustrations retain empty alt text; meaningful product images have descriptions.                                                                                                                                           |
| Mobile breakpoints             | Public sections checked at 320, 390, 1024 and 1440px. New public documents checked at 320, 390 and 1440px without horizontal overflow.                                                                                                                                                                                       |
| Sticky mobile CTA              | Appears after the hero actions leave the viewport; hides when the closing actions or footer are visible and while the cookie notice is open. Join destination verified.                                                                                                                                                      |
| Loading states                 | Existing data-route loading states retained. Sign-in renders without a streaming loading boundary so native forms remain usable without JavaScript; form submissions expose pending/busy states. Public landing content stays readable without JavaScript.                                                                   |
| Form error states              | Empty/invalid email rejected before contacting the auth service. Provider and network errors remain visible in the form. Invalid fields use `aria-invalid`; pending state clears after errors.                                                                                                                               |
| Thank-you page                 | `/thank-you` provides the email-link next step after the auth provider accepts a request. Mocked action tests cover success and failures without sending real test emails. Page access alone does not claim sign-in, account creation or delivery. Return paths reject external URLs.                                        |
| Privacy policy page            | Expanded data notice at `/privacy`, including providers, consent and account controls. Contact email added. **Operator identity and public postal address remain deferred, so this remains a draft for publication.**                                                                                                        |
| Terms and conditions           | `/terms` covers accounts, data, beta features, payments, conduct and rights. Contact email added. **Operator identity and public postal address still need completing alongside the privacy page.**                                                                                                                          |
| Cookie banner                  | Equal essential-only and analytics choices, off by default. Choice lasts up to 180 days; `/cookies` lets visitors change or withdraw it. Storage failures fall back to the current session. No inactive cookie controls shown without JavaScript.                                                                            |
| Analytics installed            | Official `@vercel/analytics` installed and Vercel dashboard enabled. Only opted-in public page views and marketing events are allowed; query strings, fragments and custom properties are excluded. **Deployment and an opted-in live visit are still required to verify actual reports.** No Plausible account is required. |
| Real contact address           | `hello@lmworldtour.app` is configured in iCloud+ and added to the local footer and legal pages. **Operator name and public postal/business address remain deferred.**                                                                                                                                                        |
| Compressed images              | Landing visuals already use AVIF/WebP and separate mobile compositions. Asset-budget tests pass; the mobile hero is about 109 KiB and desktop hero about 162 KiB.                                                                                                                                                            |

## Verification

- 39 focused unit/source tests: consent persistence, expiry and withdrawal; private URL filtering; auth callback safety; form outcomes; proxy boundaries; public metadata; route recovery; image budgets.
- 11 passing browser tests: `tests/e2e/launch-checklist.spec.ts` and `tests/e2e/landing-motion.spec.ts`, Chromium. Covers public URLs, mobile layouts, sticky actions, cookie changes, form errors, fades, keyboard focus, reduced motion and the no-JavaScript landing page.
- Selected ESLint and full TypeScript checks pass. The concurrent Course Twin task reported a successful production build with the checklist integration present; later checklist adjustments were checked by TypeScript and browser tests.
- No live email was sent to exercise confirmation. No real visitor collection or authenticated whole-app visual audit is claimed by this checklist.

Vercel describes Web Analytics as cookie-free and documents its event-filtering and deployment steps: [privacy](https://vercel.com/docs/analytics/privacy-policy), [sensitive-data filtering](https://vercel.com/docs/analytics/redacting-sensitive-data), [installation and deployment](https://vercel.com/docs/analytics/quickstart).

## Domain email setup — 14 September 2026

The user selected `hello@lmworldtour.app`. iCloud confirmed the domain is ready, the address is listed under Custom Email Domain Addresses, and its Send From checkbox is enabled. Vercel readback confirms both MX records, the Apple verification TXT record, SPF and the DKIM CNAME. Existing website records were retained. The user’s existing default iCloud sender remains selected. No additional email subscription was purchased. No manual test message was sent, so end-to-end message delivery has not been exercised.
