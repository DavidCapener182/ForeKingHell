# UI Skills installation checkpoint — 6 September 2026

The user requested all skills from https://www.ui-skills.com/ and their use in the redesign.

The live registry contains **289 entries** (287 unique source URLs). All 289 were downloaded and packaged with source provenance into `/private/tmp/ui-skills-install-20260906/review-only-catalog`. Staged resources total 4,706 files / approximately 173 MiB. Downloading and keyword screening do not constitute a complete safety review.

## Installed after individual review

- `uis-jakubkrehel-better-ui`: UI polish and its bundled Markdown guidance.
- `uis-ibelick-fixing-accessibility`: accessible names, native semantics, keyboard/focus and form states.
- `uis-ibelick-fixing-motion-performance`: compositor-oriented, interruptible motion, offscreen suspension and restrained measurement.

These three documentation-only skills were read, validated and copied using the Codex skill-installer helpers. They live in `/Users/davidcapener/.codex/skills/`. Existing skills were preserved. The names are prefixed to avoid catalog collisions; original text and source hashes are retained inside each directory. The new skills become automatically available on the next turn; their reviewed guidance was applied manually during this turn.

## Remaining installation block

Automatic approval review rejected the bulk persistent installation because activating 289 external skills at once could inject untrusted instructions into future tasks. A separate request to install only the three individually reviewed, documentation-only skills was approved. **The other 286 are staged and inactive. Do not treat staging as installation or bypass the bulk rejection by blindly installing batches.**

A complete source/status manifest is in `output/playwright/redesign/ui-skills-installation.json`; the staging and screening inventory is under `/private/tmp/ui-skills-install-20260906`. No downloaded skill scripts were executed. The installer used only its own trusted validation/copy helpers and public repository retrieval.

## Current application

Used the reviewed guidance to compact Play's preparation checklist, give its evidence links visible keyboard focus, preserve existing UI and motion libraries, and correct the caddie book's nested main landmark and heading hierarchy. Carousel geometry/motion validation remains recorded separately. Full all-route accessibility/performance verification is still pending.

## Revalidated 7 September

The289-entry installation manifest and staged catalogue still exist. All three named installed SKILL.md files are present in the active skills directory. The recorded286 inactive entries remain an unresolved user requirement; no bulk activation was retried and no downloaded scripts were executed. Registry count is the saved6September snapshot, not a fresh live-registry claim.
