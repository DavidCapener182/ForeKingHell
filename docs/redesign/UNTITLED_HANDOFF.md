# Round work handoff to the Untitled UI upgrade task

The original redesign goal remains active. The parallel upgrade task owns its root 492-component tracker; this document provides evidence for reconciliation, not completion claims for whole tracker rows.

## Ownership

Upgrade task: shared headers, shells, navigation/surface routing, global styles, new namespaced components and root audit trackers. This task: round creation/live/review, course strategy and post-round review, associated domain helpers/tests, docs/redesign and its route generator. Neither task should stage or commit the other's changes without separate agreement. Current checkout branch was changed by the upgrade task to upgrade/untitled-ui-migration; no branch switch or publication was performed here.

## Evidence mapping

| Audit item          | Implemented and evidenced                                                                                                                                                                                                                          | Still unproven / incomplete                                                                                                                                                                                                                        |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P44-C01 setup       | Requested course/tee reaches both surfaces. Inline creation errors preserve entries. Phone long-name clipping fixed. 320/390/430 overflow passes; desktop 1440 screenshot readback.                                                                | Searchable desktop selector, switching courses and incompatible tees in browser; 360/1280 and boundary viewport states, historical-entry errors and full keyboard checks.                                                                          |
| P44-C02 score entry | Live phone scoring has current hole, large controls, pending/saved status. Failure/reload/retry preserves scores.                                                                                                                                  | Historical multi-step scorecard entry and complete desktop/mobile grid verification.                                                                                                                                                               |
| P44-C03 save        | Same form retry uses stable identity. Exact POST replay creates one owned round; completion opens that record's review.                                                                                                                            | Full historical form workflow, Back/draft persistence, full state/viewport matrix.                                                                                                                                                                 |
| P45-C01 result      | Full-round achievements and feed wait for completion. Scorecard-only review labels evidence; all-par and tied scores no longer produce an invented costly first hole or turning point.                                                             | All changed summary visual/state checks after shared migration; current and completed identity parity across history.                                                                                                                              |
| P45-C02 tabs        | Existing review tabs preserved.                                                                                                                                                                                                                    | Every tab task and correction entry on both surfaces remains unverified.                                                                                                                                                                           |
| P51-C04 reflection  | Shared saved-note model; desktop recovery wrapper and focused phone post mode. Explicit unavailable IDs no longer select another round.                                                                                                            | Live failure/retry, reload, desktop readback and foreign-round protection passed in post-round-browser-pass.log; full state/viewport criteria remain pending.                                                                                      |
| P51-C05 outcomes    | Shared loader scoped to owned completed round; selected older rounds can load beyond first30. Existing directional confidence projection masks questioned lateral evidence. Round observations distinguish score, stats, cause and missing values. | Mobile/desktop measured and empty outcomes now share honest status semantics; native picker, source+club planner handoff, scoped axe/overflow verified. Full prescribed drill persistence and complete viewport/theme/state matrix remain pending. |

Verified logs before current post-round extension: round-award-browser.log (1 pass,20.1s), strategy-context-browser.log (1 pass,10.9s), round-strategy-visual-verified and strategy-phone-final for scoped axe/overflow. Current post-round unit suite: post-round-unit.log,5 files/23 tests pass. Do not use these narrow checks as proof of all audit acceptance items.

Current new/changed files include src/lib/round-learning-review.ts and its test, src/lib/post-round-review-data.ts, src/lib/post-round-review.test.ts, src/app/courses/strategy/post-round-companion.tsx, post-round-review-form.tsx, actions.ts, both strategy page compositions, src/app/(app)/rounds/[sessionId]/page.tsx, its page-source test, and tests/e2e/round-lifecycle.spec.ts. Earlier round/strategy changes are listed in CHECKPOINT.md. Source handoff replaces a roundId-only Practice link that the planner did not consume with a working selected-round reflection step; prescribed practice continuity still requires explicit completion.

Final current evidence: post-round-browser-pass.log,1 comprehensive browser test passed14.9s. Stable screenshots in post-round-verified inspected. Types/scoped lint pass. Current shared migration is owned and verified independently by the upgrade task.

## Evidence update — 6 September, 22:11

`post-round-evidence-browser-verified.log`:1 test passed8.4s using disposable local fixtures. Empty/measured post-review on companion390 and workbench1440; native round switch; missing evidence has no Measured badge or unsupported Practice CTA; actual Practice source evidence preserves selected round and club. Mobile disclosure opens with Enter and96px notes field height verified. Scoped results axe reports zero violations; no document overflow or page errors. Inspected four initial screenshots and refined compact phone screenshots. Full-page fixed bars appear at capture scroll position, so these do not establish every scroll/focus state.

`post-round-evidence-unit-final.log`:22 tests/4files passed. Types and scoped lint passed after refinement. One intermediate browser failure was a test locator matching both measured and recommendation text; exact measured-row text fixed it. No application defect was inferred from that locator failure. New results component is `src/app/courses/strategy/post-round-results.tsx`; no edits to your shared owned files. No staging or commit performed by this task.

## Historical entry update — 6 September,22:32

P44-C01/C02/C03 remain partial, with stronger evidence: `historical-round-browser-stable.log`2tests passed39.4s (historical17.8s, existing live20.7s). Historical phone enters all18scores, corrects missing/invalid values, focuses hidden invalid optional stat, cancels/accepts tee change, rejects empty tee without fabricated hole facts, preserves scores/date/notes after a deliberate server rejection, retries and opens saved review. Desktop independently creates the full scorecard with exact saved values. Request IDs now remain stable across1280/1440viewport changes; replay creates one record.

Inspected final desktop1280/1440 and phone screenshots. Desktop help rail moves to2xl so fields and all8columns are visible. One-hole companion entry verified320/390/768/1024 with no document overflow and scoped form axe zero violations. Full-page fixed chrome capture artefacts are not proof of keyboard/screenreader/scroll safety.

New server pure helper `manual-round-scorecard.ts` takes actual authorised tee holes; hidden browser pars/yardages cannot replace them. Scores/stats must be nonnegative whole numbers (score >=1); unknown optional values stay null. New tests cover missing holes, changed order/count, malformed values, canonical hole facts and par3 fairway handling.16targeted tests/types/scoped lint pass. Scope also includes local new-round-form.module.css, new-round-form.tsx, mobile-start-round.tsx, rounds/new/page.tsx and rounds/actions.ts. No changes to your shared owned modules.

Outstanding P44: full navigation/refresh draft persistence and account switching, genuine software keyboard/zoom/themes, complete selector/data combinations and broader viewport matrix. This evidence does not complete whole audit rows.

## Round corrections update — 6 September,22:52

P45 remains partial. `round-correction-browser-streaming.log`1passed7.2s verifies companion390 failure/retry/reload, direct Scorecard URL/Back, scoped scorecard axe0/overflow0; workbench1440 native disclosure and save; and two simultaneous different-hole saves preserving both changes in the actual isolated DB.3files17unit tests, scoped lint and types pass. Inspected phone/desktop captures. New pure round-hole-correction helper, owner-scoped transactional lock/recalculation and preserved unknown penalties support the flow. Phone hole edits are directly in Scorecard, compact per-hole disclosures; desktop page-local disclosures are native details. Failed save retains draft values; pending submits disable fields; unknown manual chips/sand are not fabricated as0. No edits to your owned shared modules apart from explicitly coordinated OfflineRoundEditForm ownership here.

Remaining at this intermediate checkpoint: tab retention and foreign-owner proof were subsequently verified below. Full refresh/navigation/account switching, imported putt provenance and wider route/theme/viewport/context mutation coverage remained pending. No whole P45 completion claim. Progress files now explicitly owned by upgrade task.

### Draft retention and foreign-owner verification —22:56

Upgrade task approved minimal shared MobilePageTab.keepMounted addition; only round Scorecard opts in. Final round-correction-browser-retained.log1passed11.6s now also proves unsaved correction survives collapse and Summary→Back→Scorecard, with inactive form hidden, and another synthetic owner's round rejects a forged correction without any scorecard change.4files25unit tests/types/scoped lint pass. Shared hold released; P45 remains partial for imported putt provenance, complete context/tee/shot workflows, full navigation/refresh/account switching and remaining acceptance matrix. Ledger updated accordingly.

### Manual putts retained during recalculation —23:01

P45 partial evidence strengthened: round-correction-browser-provenance.log1passed12.0s/12.8stotal. Actual stored launch shots no longer overwrite manual putts: explicit1 survives score8 recalculation, explicit blank staysnull, and an untouched legacy hole retains prior inference. New optional puttsSource:'manual' on existing scorecard JSON (approved schema.ts type-only property; no migration), assigned only by an explicit correction form field.4files27units/types/scoped lint pass. Full prior draft/concurrency/foreign-owner flow and cleanup remain passing. Shared hold released. Remaining imported provenance applies to broader ingestion and workflows; this specific correction overwrite is fixed. Context/lifecycle transitions, course relink/shot correction and broader acceptance matrix remain unverified, so keep P45 in progress.

## Context, completion and course visibility —6September,23:27

P45 remains In progress with new context/tee evidence. New round-context.ts pure helper validates explicit status/completion and preserves recorded holes when relinking. Both completion actions lock the owned scorecard and use existing completion feed dedupe; retries produce one event. Context UI disables Complete with missing scores and explains the count. Course relinking checks round ownership and shared/owned tee access, rejects empty/missing-hole tees, keeps nine-hole rounds at nine and updates canonical tee facts transactionally alongside recalculation. Native disclosure utility merging now aligns its title left.

Browser found and fixed an actual private-course picker leak in round detail. Picker uses visibility predicate; current tee metadata, live geometry and competition references also use accessible-course joins. Stale private links retain saved owned scorecard with explicit recovery text, exposing no live private course name/geometry in the tested response. No changes to your shared-owned components; local round-edit-select.tsx gained disabled option support only.

Evidence: round-context-browser-final.log2passed~1.2m (context17.0s/live51.7s); final round-context-browser-mobile.log1passed17.6s/18.4stotal includes actual phone save/readback and phone-form axe0. Desktop context/course axe0, no pageerrors, phone390overflow0; concurrent tee+hole edits preserve both. Foreign round/private tee requests rejected and exact scorecard unchanged. Final native disclosure geometry asserted within24px of left edge.3files24units/types/scoped lint pass. Desktop1440 and phone390 captures inspected in round-context-stable/round-context-mobile; fixed chrome capture artefacts and wider physical/viewport/theme gaps remain explicit. Shared hold fully released; no staging/publication from this task.

Outstanding P45: remaining shot edit/delete propagation, Map/evidence/share modes, later correction feed metadata, broader import/net/handicap provenance and complete offline/navigation/account/theme/viewport acceptance. Keep whole row in progress.

### Round UI-first update, 23:56

P45 remains partial. Desktop correction controls now inline; companion has Shots tab with club filter, longest recorded carry/total links and inline club correction. Desktop summary uses a green score panel beside scoring breakdown. New owned files round-shot-evidence.tsx, shot-club-correction.ts and round-assignments.ts; modified rounds/shots actions centralize correction. 34 focused tests pass; new full correction browser spec still pending (latest failure no save alert during concurrent compile). Do not mark P45 complete from this slice. Scope/type/browser details in CHECKPOINT.md.

### Direct user ownership transfer

David instructed the other task to take control of component fixes. All UI/component ownership is transferred to the upgrade task, including all previously owned round/strategy and companion components. This task retains underlying workflow/data-integrity services and actions. P43 history was read only; its UI remains untouched. New Round UI and its incomplete verification are detailed in CHECKPOINT.md.

### Backend evidence update — 7 September,00:08

Six direct real-Postgres integration checks now pass for club correction, raw/manual/directional provenance, stock eligibility, actual linked practice refresh, owner/session isolation, rollback/retry/concurrency and score/tee SG replacement. Actual Next actions are called with authentication/cache/flash adapters mocked; calculations/persistence remain real. Existing owned round feed score/course metadata now refreshes transactionally without changing audience, headline or original date. Types and30focused tests pass. This does not resolve the UI/browser save-alert issue or finish P45 acceptance. Action APIs unchanged; UI ownership stays entirely with upgrade task.

### Backend correction handoff — 7 September, 00:27

All UI remains with the upgrade task. Backend now verifies atomic whole-club rename/merge/reassignment, derived evidence refresh and saved-warning/retry semantics. `correctShotClubAction` and `updateShotClubAction` resolve `{previousClubId, warning?}`; `updateClubAction` resolves `{warning?}`. Retain first Undo target through warning retries. Offline endpoint success body also includes optional `warning`; queue/UI must preserve/display it. Do not label a committed correction as failed solely because practice/awards refresh failed.14realDB+5lifecycle checks pass; these do not close browser/UI verification gaps. Completion-event replay preserves chosen feed audience; explicit visibility action unchanged. All affected owner rounds/practice/stock are refreshed after bulk club changes, not only the currently open round. No UI edits or publication by this task in this batch.
