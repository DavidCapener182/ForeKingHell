# iPhone companion implementation ledger

Baseline: main `8efd2b6b`; refreshed origin/main on 5 September 2026 and confirmed 0 commits ahead / behind. The pre-existing `.github/workflows/nightly.yml` modification is unrelated and retained. The initial implementation shipped in PR #36 as `93b93556`, with all required GitHub checks passing. The continuation below is local work after that release; it is not a deployment claim.

David's brief stops during section 23. The subsequent instruction authorises finishing the iPhone experience and mobile polish by judgement. Desktop, authentic account data, calculations, and valid deep links remain the boundaries.

## Architecture audit

Inspected the route inventory (97 page routes), source structure (1,451 source files at baseline), tests, domain boundaries and recent implementation history. App Router entrypoints live in `src/app/(app)`; feature implementations also live in `src/app`. `proxy.ts` applies companion handoffs using `app-route-capabilities.ts`. `PrivateAppShell` selects companion/workbench, and route metadata owns navigation/back links. An explicit workbench choice remains authoritative.

Drizzle schema, domain services and authenticated server actions remain authoritative for shot lifecycle, stock yardages, speed transfer, practice prescriptions, training load and scoring. Releases #31 and #32 supplied existing shot-integrity and speed-evidence behaviour. The mobile work consumes those calculations rather than replacing them.

## Implemented coverage

| Brief                           | Implementation                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–3: companion and navigation   | Today, Practice, Play, Progress and Bag are the five persistent destinations. Sessions, shots, import, settings and secondary tools remain available under contextual navigation. Existing deep links are retained.                                                                                                                                                                                           |
| 4: shell                        | Contextual compact titles/back actions, native large titles, profile entry, stable safe-area tabs, saved tab scroll, keyboard adaptation and immersive activity chrome. Existing accessible Radix/Vaul sheets keep focus and scroll locking.                                                                                                                                                                  |
| 5–6: design system              | Semantic green light/dark tokens, Apple system-font stack, type roles, readable metrics, touch targets, restrained surfaces and reduced-motion support. Grouped primitives have one implementation with compatibility exports for existing callers.                                                                                                                                                           |
| 7: Today                        | One evidence-backed focus, greeting/date, current saved activity, next action, meaningful measured change and compact recent activity. Low sample counts are explicit.                                                                                                                                                                                                                                        |
| 8: Practice                     | Recommendation, quick duration changes, intent/facility/energy sheet, block preview and a single start/resume action. Failed regeneration retains the previous plan.                                                                                                                                                                                                                                          |
| 9: Range Mode                   | Immersive instruction-first screen, success target, swipe/previous/next, ball counter, block progress, notes, pause and finish. Wake-lock/haptic hooks are capability-dependent. Full active plan, selected block, counts, notes and completion survive local recovery; progress retries online. Activity completion remains distinct from measured success.                                                  |
| 10: Quick Range                 | Single-screen club/focus/balls/optional-target setup; timer and manual shot context; local recovery; measured import, note and review handoffs. Practise-this-club links preselect the club.                                                                                                                                                                                                                  |
| 11: Speed                       | Existing playing speed, verified PB, seven-day average and comparable-session trend, target and labelled carry projection; staged training, measured entry, rest timer, fatigue guidance, target ladder and history. PB feedback follows saved evidence. Ball-transfer quality remains separate from raw speed.                                                                                               |
| 12: Play                        | Selected course/tee, real readiness/last played, preparation, Course Twin, Start Round and Quick Bag. Only missing setup is surfaced. Chronological recent rounds replace dashboard clutter.                                                                                                                                                                                                                  |
| 13–14: strategy and Course Twin | Mapped one-hole strategy, personal dispersion, evidence-supported Safe/Normal/Aggressive, hole navigation/swipe. Course Twin retains dynamic scene loading, mobile quality tiers, resolution/shadow controls and fallback. Mobile modes read Plan/Play/Replay. Explicit Plan/hole links now survive loading an older resumable round.                                                                         |
| 15–16: rounds                   | Short course/tee start flow; outdoor score/putt/penalty controls, optional fairway/GIR/notes, hole swipe and running score. Serial account-scoped offline saves retain operation IDs and version preconditions. Completion drains pending holes before marking the round complete. History and Summary/Scorecard/Map/Insights retain existing scoring calculations; advanced correction remains in workbench. |
| 17: session review              | Session metadata, verdict, measured pattern, swipeable metric story, signals/change/next practice and direct shot exploration. Heavy measured charts load on demand.                                                                                                                                                                                                                                          |
| 18–19: shot evidence            | Lightweight shot rows, club/session/trust/type/search/sort filters, detail sheet, review suggestions, reasons/confidence, reversible exclusion/restore and batch review. Club correction validates ownership, records provenance and refreshes both clubs' trusted stock. Raw measurements and source rows are retained. Existing importer/statistical classifications remain authoritative.                  |
| 20–21: bag                      | Distance-ordered trusted club ladder, compact range bars, rounded phone metrics, club detail/evidence/next practice. Quick Bag has Carry/Total and account-scoped offline caching.                                                                                                                                                                                                                            |
| 22–23: progress and training    | Performance/Scoring/Training/Goals story using existing scores and confidence. Training Over Time has range selection, fitness/load/form, compact chart, evidence and recent sessions.                                                                                                                                                                                                                        |
| Offline/account boundaries      | Public offline shell preloads its static assets and recovers same-account Quick Bag, Range Mode, Quick Range and scoring. Private HTML is not cached. Signing out/account switching purges private companion storage.                                                                                                                                                                                         |

## Data and capability boundaries

- No new demo data is shipped. Empty, unavailable, modelled and low-confidence evidence remain labelled.
- Automatic review now combines existing triage with conservative wrong-club suggestions: paired carry/speed, one unique alternative, at least 20 trusted samples from two other sessions, matching equipment identity/source/context. Intentional chips and pitches are classified without filling the suspect-data queue. Suggestions never change data automatically. Keep records provenance and supports guarded Undo; exclusions and club corrections remain reversible.
- Plan-versus-actual, measured replay, weather, strike quality and handicap effects use available linked evidence. No planned/actual shot pair or performance improvement is manufactured from manual completion.
- Screen wake lock, vibration and device offline storage depend on browser capability. WebKit emulation verifies rendering, not physical iPhone haptics or battery consumption.
- Full authenticated production mutation tests were not run against the personal account. Round retry/completion QA used isolated local fixtures with intercepted requests. Existing data-lifecycle tests cover server/domain paths.

## Verification — 5 September 2026

- Full Vitest suite: **466 files, 2,242 tests passed**.
- TypeScript: `npx tsc --noEmit` passed.
- ESLint: all changed/new TypeScript and TSX files passed without warnings.
- Production Next build passed. Every configured route bundle budget passed; heavy practice result and range modules, and session charts, were split instead of raising budgets.
- Chromium and WebKit iPhone checks: Today, Practice, Range Mode, Play, Progress, Bag, Quick Bag, club detail, session review, shots, new round, speed, training and mapped strategy. Verified 390/393-pixel layouts; targeted 320-pixel checks for Practice, scoring setup, shot/speed surfaces and strategy. Inspected light/dark and reduced-motion states.
- Strategy: next hole and Safe selection work; no horizontal overflow at 320 pixels. Course Twin Plan link preserved its mode after loading the saved round; rendered mapped course and personal shot overlay without horizontal overflow.
- Isolated scoring interaction: offline score/note persistence and reopening passed; missing-score completion refused; simulated uncertain response retried the identical operation envelope; completion followed all hole acknowledgements.
- Production service worker: cold offline navigation recovered Quick Bag, including Carry/Total, from the public offline shell.
- Desktop workbench: shot table remains present, mobile tab bar absent, and explicit surface selection retained. An existing Radix checkbox style hydration warning was observed in development; the table remained functional.
- `git diff --check` passed. Alternate-build generated `tsconfig.json` edits were removed. Unrelated nightly workflow change retained.

Screenshots are in the ignored local `output/playwright` folder. Representative evidence: `iphone-today-light-settled.png`, `iphone-practice-final.png`, `iphone-range-mode-final.png`, `iphone-club-detail.png`, `iphone-session-review.png`, `iphone-strategy-final.png`, `iphone-course-twin-settled.png`, `iphone-offline-quick-bag.png` and `desktop-shot-regression.png`.

## Continuation checklist

- [x] Previous turn classified as verified implementation progress.
- [x] Implement automatic wrong-club review without altering raw data or authoritative calculations.
- [x] Add owned Keep/Undo and batch review with audit provenance and conflict guards.
- [x] Add explicit route registration and preserve deep navigation.
- [x] Add the missing seven-day speed trend without mixing measurement contexts.
- [x] Verify real-account read-only queue rendering in WebKit; no account review mutations made during UI QA.
- [x] Re-run complete verification after the automatic-review addition: 466 files / 2,242 tests, TypeScript, ESLint, production build and all configured route budgets passed. Added budget coverage for automatic review and Speed.
- [ ] Audit individual remaining acceptance details beyond the implementation coverage summary.

### Acceptance details still under review

- [x] Today now combines owned sessions, saved imports, completed/reviewed plans, achievement unlocks, recorded PBs and subsequent goal-value edits. Events retain their actual dates; badge backfills cannot fill the entire six-row list. Existing goals are not given invented historical movement.
- [x] Active-round Play now links Strategy to that round's own course, tee and first unscored hole. Missing course identity does not produce an invented strategy link. The phone layout uses a single score strip and two secondary actions.
- Inspect remaining immersive-flow details, club evidence consistency, and secondary settings/error states individually before claiming the full brief complete. The coverage table above is not proof of every acceptance detail.

## Continuation after PR #36 — 5 September 2026

Previous goal turn: **progress**. PR #36 merged after required checks; local main was verified at remote parity, retaining the unrelated nightly workflow change.

- Added the mixed Today timeline using owner-filtered, bounded database reads. Goal-value history is stored with preferences in the same transaction/row lock, retained to 48 events, removed when a goal is deleted, and labelled as a value update rather than measured improvement.
- Added owned `/practice?planId=…` review links. Lookup addresses the requested plan directly rather than searching only the most recent 25. Invalid or inaccessible links fail closed. Completed plans render read-only and cannot overwrite an active practice cache.
- Added the active-round Strategy shortcut and removed the old dark promotional treatment from that phone panel. Desktop round presentation and scoring calculations are unchanged.
- Strengthened the collapsed navigation material for readable text over scrolling content. Historical-plan URLs now have contextual back navigation and separate saved scroll state.
- Closed a round-completion edge case: an active round with every hole scored stays in scoring until completion is acknowledged. It opens on the final hole and waits for cache restoration before enabling navigation. Penalties were added to the mobile result strip; live strategy retains tee identity.
- Explicit session-specific Course Twin entries now request Replay. WebKit verified the selected round's measured-launch/reconstructed-flight view, Play/Pause replay, and the low-power 2D fallback. Screenshot: `output/playwright/iphone-round-replay-verified.png`.
- PR review follow-up: a requested round without eligible replay evidence now opens Strategy. Page-rendering tests cover scorecard-only links, explicit Replay without evidence, measured Replay, an explicit Strategy override, and ordinary course entry.
- Verification: 470 Vitest files / 2,258 tests passed; TypeScript, changed-file ESLint, production build and all existing route budgets passed. No budgets were increased.
- WebKit: real-account Recent rendered mixed event types; the saved Driver delivery plan opened and survived refresh; the 320-pixel review had no horizontal overflow. The initial 30-second review wait expired during a 36-second cold development compile; the same navigation subsequently returned 200 and rendered correctly. No account goal or shot data was mutated in browser QA.
- Local screenshots: `output/playwright/iphone-today-timeline-settled.png` and `output/playwright/iphone-saved-practice-review.png`. Browser cache readback was unchanged across review refresh; a physical-device active-session lifecycle remains a separate check.

### Concrete remaining acceptance findings

These findings supersede any implication that the broad coverage table proves the entire brief.

| Requirement                              | Current evidence                                                                                            | Remaining work / proof needed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4: predictable back behaviour            | Primary roots and path-based detail back links exist. Saved practice now uses a query on the Practice root. | Contextual Back to Practice now works; WebKit verified the title sits below the bar and the link returns to /practice. Query-specific scroll storage is separate from the Practice root; full tab restoration audit remains.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 9: Range overflow controls               | Shared Range Mode now has a Session options sheet with Pause, Skip and Finish.                              | WebKit verified Skip leaves completion unchanged and final-block Skip opens finishing. Cold-offline Chromium verified selected block, note and ball count survive reopening. Physical screen-awake/haptic behaviour remains device-dependent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 11: speed session detail                 | Stages, measured entry, rest countdown and fatigue copy exist in `mobile-speed-session.tsx`.                | Pause/resume timing, block-associated readings, 75-second recovery before the second maximum block, rest extension, persisted Undo and the existing 4% fatigue rule are verified. Finish speed work becomes primary at the fatigue threshold. Build/Max/Cool-down stages now preserve the original volume and preparation classification. A dedicated mobile history review and exact five-shot transfer picker retain the existing corridor rule; explicit linked failure overrides maximum work on mobile. Real-account not-linked/empty-candidate states and an isolated picker interaction are verified. Saved-link mutation and passed/failed rendered account states remain subject to broader acceptance coverage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 13: strategy                             | Mapped one-hole strategy with personal dispersion and choice controls is implemented.                       | The phone decision panel now uses the selected option’s own measured carry/lateral evidence. URL hole/option refresh, remembered course/tee, light/dark rendering, 44-pixel controls and desktop preservation are verified. Targets remain indicative because the map has a reference tee rather than tee-set coordinates. Saved caddie books now reopen through the public offline shell with cached vector geometry, per-option evidence, selected-hole recovery and unchanged download dates. Cold-offline production Chromium, WebKit account-change clearing and older-format parser coverage are verified. Other-course and physical-device strategy checks remain open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 14: replay                               | Course Twin contains replay mode and measured shot selection.                                               | Session-specific links now supply initial Replay mode, so loading a resumable round cannot override them. WebKit verified the selected session, measured-launch provenance and replay controls. The phone low-power fallback now provides a one-hole mapped replay using the existing measured session and reconstructed path, with play/pause, scrubbing, shot/hole selection, URL recovery and reduced-motion results. WebKit verified the real Bootle round at 320 pixels without loading Three.js/terrain assets. Historical planned-versus-actual pairing and the wider Course Twin interaction/performance audit remain open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 15: live scoring strategy                | `mobile-live-round.tsx` links Hole strategy using course and hole.                                          | The tee identity now travels through the live link and saved offline context; legacy snapshots without a tee do not invent one. Live scoring now shows the current trusted-bag Plan and linked Actual club sequence for an exact course/tee match. Offline context retains the comparison; missing actual shots and unknown ordering are explicit. This is current strategy, not a captured historical pre-round choice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 15: round completion                     | `MobileRoundResultCard` shows final score, to-par, putts, differential and next practice.                   | Penalties now appear in the immediate summary. Fully scored active rounds remain finishable after refresh instead of falling prematurely into review. The result now shows before/after course or simulator estimates from the existing handicap calculation at that round, with method details and explicit unavailable/first-estimate states. WebKit verified the real Aintree simulator round at 320 pixels.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 16: round history                        | Chronological records and detail tabs are present.                                                          | Mobile history now uses a dedicated grouped list with exact-context handicap movement, factual verdicts and a completed-score trend restricted to matching course/simulator context and hole count. WebKit verified the real-account list, course filtering, older archive and desktop table preservation. Advanced correction is explicitly labelled Desktop. The real Bootle round now has verified Summary/Scorecard/Map/Insights navigation, 18 score rows and a loaded projected-shot map. Mobile Insights now leads with the existing scoring leak and next practice; proof and editing remain disclosed. Other round-detail states remain under audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 17–19: measured review and shot evidence | Prior pass plus automatic-review tests cover these workflows.                                               | The mobile explorer now has a compact search and filter sheet, unit-labelled detail metrics with missing sentinels omitted, and full-precision smash. Current-result selection is bounded and resets across pages. WebKit verified actual combined filters, carry order, context-preserving search, details, exclusion/restore cancellation, a blocked-save failure, empty results and desktop preservation. Review suggestions now have a distinct exclusion action. Successful personal-account writes were deliberately not performed; existing action tests cover those server paths. Session review now has URL-backed per-club metric stories, exact club/session shot links, estimated club-speed disclosure and a chart that uses the authoritative trusted IDs. Actual included/excluded 7-iron evidence and on-demand chart loading were verified. The other mobile pattern callers now use the existing reviewed selections; real-account Today and import patterns are checked. Import results now use one primary action, grouped comparisons and optional chart/audit details. Saved-session identity now comes from owned metadata rather than joined shots, with an empty-activity view and strict measurement scope. Real score-only round aliases and missing-session recovery are checked; the account has no empty practice record, so that composition is verified with isolated fixtures. Wider session/review acceptance remains open. |
| 20–21: bag                               | Current ladder, club detail and Carry/Total cache exist.                                                    | Club detail now derives supporting metrics, last verified date and recent sessions from its carry evidence window. Actual neighbour gaps and the sand-wedge full/touch split are implemented and checked on real account data. Quick Bag now uses the same trusted window for carry, total, range and measurement date, with a shared online/offline list and account-validated snapshots. Real-account cold-offline reopening, Carry/Total parity and desktop cache preservation are verified. The companion ladder now uses equipment-specific Quick Bag evidence, with stable duplicate-family identities and matching detail samples. The real account has ten active clubs and no duplicate families; multiple-record rendering is regression-tested. Target now distinguishes measured carry from the existing rounded course number. Broader duplicate-equipment and target interaction coverage remains open; no environmental adjustment without adequate evidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 22–23: progress/training                 | Four progress sections and training-over-time are implemented.                                              | Progress now separates the existing composite score from measured direction between distinct sessions, with explicit early/missing/mixed signals. Actual nine-hole totals, matched scoring contexts, recorded scoring focus, 28-day training consistency, stale-log guidance and saved goal evidence are implemented. Real-account WebKit verified phone light/dark views, chart controls, session navigation and desktop preservation. The account has no saved goals; populated goal rendering and broader low-data states remain to be exercised directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Global quality                           | Shared mobile tokens, sheets, safe areas, dark mode and reduced motion are implemented.                     | Complete a route-by-route rendering/interaction audit including profile, settings, errors, keyboard and immersive transitions. Physical iPhone wake lock/haptics remain capability-dependent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Continuation after PR #37 — 5 September 2026

Previous goal turn: **progress**. PR #37 merged as `fe2a266f` after all required checks and a verified fix for replay links without measured evidence. Local main matched origin/main; the unrelated nightly workflow edit remains preserved.

- Range Mode has a short Session options sheet for Pause, Skip and Finish. Skip never adds a completed block; Previous returns to unfinished work. Skipping or completing the final block opens the evidence/finish sheet. Swipes starting on inputs, links, buttons or note disclosure controls do not change blocks.
- Removed the duplicate cold-offline practice controls. The offline shell statically includes the same Range Mode component so its controls are cached with the public shell. Completed offline activity is read-only. Notes, ball counts, selection and completion stay in the existing account-scoped cache.
- Online and offline practice share the activity hook for immersive chrome and the screen wake-lock lifecycle. Completion feedback is provided by the shared Range Mode component. Physical iPhone haptics and power behaviour have not been measured.
- Round summaries show the course or simulator estimate before and after the selected round, using `getCurrentHandicapRounds` and the existing `calculateHandicapSummary`. Later rounds and the other measurement context are excluded from that historical comparison. First estimate, no change and unavailable evidence are explicit. This is an app estimate, not an official Handicap Index. Calculation details are disclosed on tap.
- Removed truncation and uppercase tracking from mobile round metric labels; Differential is readable at 320 pixels.
- WebKit local fixture QA: Skip preserved incomplete state; reopening retained block 2; final Skip opened finishing; finished review exposed no completion controls; all sheet actions measured at least 44 pixels high; no horizontal overflow at 320 pixels.
- Chromium production QA: using the actual service worker and a fresh loopback origin, cold offline navigation recovered the shared Range Mode. Skipped block 2, its note and 11 remaining balls survived a second offline reload. Fixture storage was removed and networking restored after QA; no account records were mutated.
- WebKit real-account round read: Aintree displayed simulator estimate 3.1 → 3.9, up 0.8, with the nine-score method and 9-hole equivalence available on tap. No horizontal overflow at 320 pixels.
- Screenshots: `output/playwright/iphone-range-options.png`, `iphone-offline-practice-review.png`, `iphone-range-cold-offline.png`, and `iphone-round-handicap-effect-final.png`. These are local ignored QA artifacts.
- Verification: 472 Vitest files / 2,266 tests passed after the shared-hook consolidation. Changed-file ESLint, TypeScript through the production build, the production build, all existing route budgets and `git diff --check` passed. No route budgets were increased. The full redesign remains active; the concrete acceptance table above still contains unresolved requirements.

## Live scoring and speed continuation — 5 September 2026

Previous goal turn: **progress**. The Range Mode / handicap additions remain local and verified; the full redesign goal is unchanged.

- Live scoring now loads the existing strategy only for active companion rounds with a course and tee. A fallback course or tee cannot supply the comparison. Plan uses personal carry evidence and the existing follow-up-club calculation; Actual uses only owned, linked shot records. Ordering uses one consistent numbering system; incomplete ordering is labelled without sequence arrows. The comparison is labelled current strategy, not a recorded pre-round decision.
- The compact comparison travels in the offline round context, separate from score-edit payloads. Old snapshots remain valid. Local WebKit fixture QA verified Plan/Actual, no-linked-shots messaging, hole changes and retained offline context at 320 pixels. Fixture storage was removed afterwards.
- Speed readings now retain their block key. Paused time is excluded from elapsed activity; elapsed time and rest deadlines survive reload. Undoing the final reading also clears it from the cached draft. Existing warm-up/maximum classifications and server evidence calculations are preserved.
- The second maximum block starts a 75-second recovery timer within the existing 60–90-second prescription. Between-reading rest remains 45 seconds, now visible in the instructions; +30 seconds extends the actual deadline. The existing two-readings / 4% fatigue rule now produces guidance and a primary Finish speed work action. Further measured logging stays available through a disclosure.
- Active speed entry uses one block title, open content, an 80-pixel input with 40-pixel numerals, and progressive disclosure for readings/notes. Active navigation starts at the top of the block. WebKit's unintended default Details summary was removed after visual inspection.
- WebKit speed QA used local temporary readings with POST requests blocked. It verified pause/resume, persisted Undo, three block-associated readings, recovery timing, extension, reload and 320-pixel overflow. The original draft was restored after QA; no speed-session records were submitted.
- Local ignored screenshots: `iphone-live-plan-actual.png`, `iphone-speed-entry.png`, `iphone-speed-rest-and-fatigue.png` under `output/playwright`.
- Strategy lookup now resolves explicit course and tee links beyond the bounded picker lists, preserves their identity and fails closed for invalid selections. Current bag strategy keeps the newest stock evidence per active club instead of allowing older rows to overwrite it. Four regression tests cover selection scope and stock ordering.
- Final verification: 475 Vitest files / 2,280 tests, TypeScript, full ESLint, changed-file formatting, production build and all existing route budgets passed. The final WebKit speed replay passed after disclosure-label cleanup. No budgets were increased. Broader acceptance work remains open in the table above.

- PR #38 review follow-up: a draft restores only into the matching plan title; changed readiness cannot relabel old readings. The fatigue stop stays triggered after any qualifying consecutive pair, including after a subsequent faster reading. Progressive builds are not retrospectively classified against a later peak. Regression tests, TypeScript, changed-file ESLint, production build and route budgets passed after these corrections.

## Round history continuation after PR #38 — 5 September 2026

Previous goal turn: **progress**. PR #38 merged as `5701d1f7` with required checks passing. This continuation remains local; the unrelated nightly workflow edit is preserved.

- Reconnected and consolidated the existing `RoundsMobileList` as the companion history. Desktop keeps `RoundsScoringIndex` and its table. Phone history uses the shared segmented control, grouped chronological rows, search/data sheet, older-history disclosure and a compact tappable scoring chart.
- Score, to-par, date, tee, factual verdict and context-specific handicap movement are visible per round. Partial scoring stays visible against only scored-hole par, but unfinished/invalid scorecards cannot establish a completed result or a handicap differential. Completed counts now use actual scorecard completion.
- Historical handicap movement reuses `calculateRoundHandicapEffect`; simulator scores no longer alter course estimates or vice versa. The existing differential and handicap calculation functions are unchanged.
- The mobile trend selects the latest completed 9- or 18-hole result and compares only the same hole count and course/simulator context. It shows up to eight real scores with direct review links, chronological ordering and explicit context. It never compares a partial round to full-round totals.
- Removed unsupported pressure/causation wording from scoring verdicts. Verdicts report saved penalty strokes, the highest over-par hole, level/under par or the number of pars-or-better.
- The shared mobile filter sheet now has a 44-pixel Done control. WebKit verified opening, body scroll lock, search with no results, Clear filters, closing and scroll release after the closing transition. The older archive restored all 11 owned history records.
- At 320 pixels, real-account history had no page overflow; chart touch targets measured at least 44 by 104 pixels. The latest Aintree simulator row shows +0.8 estimate movement, consistent with its detail calculation. The Real filter selects the separate 9-hole course trend. Long course names wrap.
- Desktop readback at 1440 pixels showed one table, 12 rows including its header, zero companion rows and no page overflow. WebKit also verified Summary/Scorecard/Map/Insights and the explicit Desktop correction link to the selected round’s workbench. A development hydration warning occurred when a native details element was opened before hydration (the differing attribute was `open`); the complete disclosure and link remained usable. This is recorded for the remaining global lifecycle audit.
- Dark-mode observation: an initial media-emulation screenshot retained dark foreground text after previous theme/viewport mutations. A fresh dark load and repeated light/dark switches then rendered the expected dark/white foreground correctly. No causal code fix is claimed for that initial observation; physical-device appearance transitions remain in the global acceptance audit.
- Verification: 476 Vitest files / 2,285 tests, changed-file ESLint, TypeScript through production build, production build, all unchanged route budgets and diff whitespace checks passed. Five new evidence tests cover partial/invalid scores, context/hole-count filtering, chronological recent selection and factual verdicts.
- Local ignored screenshots: `iphone-round-history.png`, `iphone-round-history-course.png`, `iphone-round-history-dark-reload.png`, `desktop-round-history-preserved.png` in `output/playwright`. No account round, goal or shot records were changed during QA.

## Club evidence continuation — 5 September 2026

Previous goal turn: **progress**. Round history changes remain local and verified; this continuation adds club-detail evidence without discarding them or the unrelated nightly workflow edit.

- The companion club profile now derives total, ball speed, launch, lateral median/range, sample count, latest evidence date and recent session links from the same trusted selection as its carry hero. Full swings use the existing latest-reliable selector; touch uses an extracted shared selector from the unchanged short-game calculation. Excluded raw shots cannot advance Last verified or supply the usual miss.
- Missing lateral evidence says Not measured rather than Mostly straight. Dispersion is explicitly the middle-half lateral width, not a claimed full miss envelope. Stock confidence retains the existing score and uses the same high/moderate/building presentation thresholds as the mobile ladder.
- Active owned clubs supply actual nearest longer/shorter gaps from current reliable carries. Club, shot and session reads retain owner conditions; archived equipment is excluded. Equal-timestamp shots use shot-number ordering consistently. Same-type duplicates are not offered as neighbours; a broader audit of merged active-club families remains open.
- Sand-wedge detail now leads with the full-swing carry shown by Bag. Pitch/chip touch remains available in its own section and never participates in full-swing gapping. Other touch-only clubs retain their touch presentation.
- The latest three sessions supplying the displayed window link directly to session review. Readable course/session labels replace import filenames, with exact dates and contributed-shot counts. Existing raw-shot access and advanced analytics remain available.
- WebKit real-account readback at 320 pixels: 7 iron 145 yd (137–150 range), 6 iron 152 yd (+7), 8 iron 142 yd (−3), latest evidence 22 August 2026. Sand wedge 82 yd full swing (80–86), 50 yd touch from 73 shots, and GW 102 yd (+20). Light/dark readbacks had no page overflow; dark heading text was white. No account records were submitted or altered.
- The 7-iron practice shortcut opened Quick Range with Club = 7 Iron and Focus = 7 Iron control. A recent-session link opened the correct review. That readback exposed two visible headings, Session review and MIXED; remove the redundant session heading in the remaining session-review audit.
- Desktop readback at 1440 pixels showed the existing desktop analysis profile, zero mobile profiles and no page overflow. Development Fast Refresh interrupted cold navigation during QA; settled navigation subsequently passed without restarting a live server to treat the interruption as success.
- Validation: full suite passed 477 files / 2,289 tests; changed-file ESLint passed. TypeScript caught an invalid test fixture status, corrected to user_excluded; the evidence/short-game tests and TypeScript then passed. The final production build and all unchanged route budgets passed. No budgets were increased.
- Local ignored screenshots: `iphone-club-detail.png`, `iphone-club-evidence.png`, `iphone-sand-wedge-detail.png`, `iphone-club-detail-dark.png` and `desktop-club-profile-preserved.png` under `output/playwright`.

## Session review release continuation — 5 September 2026

- Companion review now has one session title, a concise verdict from existing comparison results, evidence disclosure, a swipeable metric story, grouped findings and a contextual Quick Range action before the shot chart. Desktop retains its report composition.
- Each metric uses its own available trusted readings. Missing side data no longer removes measured carry; unavailable metrics are omitted. Dispersion explicitly describes the middle 80% of measured lateral readings.
- A linked practice club absent from the session can no longer hide the metric story: focus falls back to a measured trusted club. Six regression tests cover independent readings, missing values, supported verdicts, practice context and focus selection.
- Real-account WebKit verification at 320 pixels confirmed one heading, six measurable metrics, working Previous/Next navigation, the exact session-filtered Shots link, the contextual 7-iron practice link and no page overflow. No account records were changed.
- Full suite: 478 files / 2,295 tests; TypeScript and full ESLint passed. The complete mobile brief remains open against the acceptance ledger above.
- Release checks: dark-mode session review rendered correctly; the desktop report remained present at 1440 pixels with no page overflow after settled navigation. Production build and all unchanged route budgets passed. Generated build-only TypeScript configuration changes were restored. The unrelated nightly workflow is excluded from this release.
- PR #39 review fixes: both supported live statuses (`active` and `in_progress`) stay out of completed scoring evidence. Recent club evidence preserves session type and uses the shared review router so rounds open their scorecard rather than practice review. Regression coverage passed with 478 files / 2,296 tests; TypeScript, changed-file lint, production build and unchanged route budgets passed again.

## Quick Bag evidence and cold-offline continuation — 5 September 2026

Previous goal turn: **progress**. PR #39 merged as `c36ed69f`, with local main matching origin/main and the unrelated nightly workflow retained. This continuation remains local and uncommitted.

- Companion Quick Bag now calculates carry, quartile range, total, sample counts and the last measured date from the same existing trusted selection as club detail. A newer excluded shot cannot advance that date. Sand-wedge full swings remain separate from touch. Empty clubs retain unavailable values. Account conditions cover the equipment, shots and joined sessions; only compact results reach the client.
- Carry/Total share one distance-ordered list online and offline. A club disclosure shows equipment, trusted sample, measurement date and confidence without importing the heavier desktop drawer. Total uses its own available-reading count. Online club-detail links remain available; offline evidence requires no network-loaded detail component.
- Only the selected companion/workbench client renders. The existing desktop target/search interface retains its stored-stock calculation and no longer replaces a newer companion snapshot. Explicit workbench selection remains renderable at small widths.
- Version 4 snapshots carry an account identity and saved timestamp, validated separately from the measurement date. Unknown versions, malformed data, duplicate IDs, invalid dates and mismatched accounts fail closed. Version 3 yardages remain readable as earlier snapshots, with their unverifiable evidence dates omitted. Offline reads never renew the timestamp or rewrite the snapshot. Service-worker cache version advanced to refresh the changed offline shell.
- Real-account WebKit at 320 pixels: Driver 190, 5W 172, 5i 154, 6i 152, 7i 145, 8i 142, 9i 127, PW 113, GW 102 and SW 82 yd carry. The 7-iron disclosure shows 20 trusted full swings, 22 August measurement date and the existing 79% stock score. Total shows 153 yd from its 19 available readings. Online/offline rows matched exactly; light/dark layouts had no page overflow.
- Desktop WebKit at 1440 pixels retained target matching (150-yd target → 6i, stored play number 155 yd), with one desktop surface, zero mobile surfaces and no overflow. The companion snapshot remained byte-for-byte unchanged. QA waits for client hydration before entering the target. Development Fast Refresh interrupted initial cold navigation; settled retries passed without restarting the server as a workaround.
- Chromium cold-offline verification used the real yardage snapshot in the isolated local production origin. With the server deliberately stopped and an uncached network request confirmed to fail, navigation to Quick Bag recovered the cached public shell. Carry/Total, club evidence and full reload worked with ten clubs, no overflow and the snapshot unchanged. A preceding online-flag observation was inconsistent and was not accepted as sufficient offline proof.
- Verification: 480 Vitest files / 2,302 tests passed; TypeScript and changed-file ESLint/formatting passed. Initial standalone typecheck overlapped Next's dev type regeneration and reported missing generated files; the settled rerun and production build passed. The initial route budget exposed a 1-KiB Quick Bag overage. Keeping offline-only snapshot parsing out of the normal page restored all unchanged budgets. No budget increased.
- Local ignored screenshots under `output/playwright`: `iphone-quick-bag-evidence.png`, `iphone-quick-bag-detail.png`, `iphone-quick-bag-dark.png`, `iphone-quick-bag-offline.png`, `iphone-quick-bag-cold-offline.png` and `desktop-quick-bag-preserved.png`. No account records were submitted or changed. Duplicate active-club family handling and the broader mobile acceptance ledger remain open.

## Speed stages and session review continuation — 5 September 2026

Previous goal turn: **progress**. Quick Bag evidence and cold-offline changes were implemented and verified locally. They remain uncommitted alongside this continuation; the unrelated nightly workflow is preserved.

- Full speed sessions present Warm-up, Build, two Max intent sets, Transfer to ball and Cool-down/course rhythm. The original six progressive rehearsals are presented as three warm-up and three build swings; maximum sets, ball counts, duration and the existing rest prescription are unchanged. Transfer/technical days do not gain maximum work. Build readings stay in the preparation phase and cannot trigger maximum-speed fatigue or PB evidence.
- Drafts store stage identity. Older block indices map through the original plan so inserting Build cannot move a saved second maximum block into the wrong stage. The existing plan-title guard remains. Current-block access tolerates the shorter plan supplied after a save.
- Speed history now opens a dedicated companion review, with one title, session peak, median/top-three average, a concise verdict, linked transfer status, a disclosure for individual swing rows and the saved note. Desktop retains its swing table, fatigue chart, edit form and transfer editor behind an explicit workbench handoff.
- The companion transfer picker uses the existing authenticated server action and candidate rules. It requires exactly five unique selected shots, resets selection when changing sessions and explains unlinking without deleting speed or shot records. The existing four-of-five lateral corridor calculation is unchanged; the UI explicitly avoids presenting lateral success as proof of strike quality.
- An explicitly failed latest linked transfer takes priority over a general positive speed trend on mobile: the status/copy and next prescription focus on control and transfer, removing maximum-speed blocks without adding swings. Recovery/technical plans remain authoritative. Missing evidence is not converted into a failed result.
- Grouped mobile rows now use the existing `AppSurfaceLink` for `/surface/` destinations. These use document navigation and cannot prefetch a cookie-changing surface switch before the user taps. Ordinary content links retain prefetching. The real phone-to-workbench handoff passed after this change.
- Real-account WebKit at 320 pixels: Speed retains the 92.9 mph verified PB and 89.3 mph playing-speed evidence. Driver R-Speed review shows its separate 87.0 mph session peak, 84.0 median, 86.3 top-three average and 19 swing readings. One title, zero phone tables and no horizontal overflow were verified in light/dark views. The real account has no eligible nearby five-shot candidate for that session; the import/empty state was preserved.
- The active-stage walkthrough used temporary local browser readings only, then restored the previous draft. Warm-up/Build were classified as preparation; two qualifying drops triggered the existing 4% fatigue stop; Cool-down had no maximum-speed entry. No speed session was submitted to the account.
- The actual transfer-picker component was exercised in an isolated local test page with its account action disabled. Five unique selections enabled linking, six disabled it, changing candidate cleared them, and submitted-form field inspection identified the exact speed session, ball session and five shot IDs. Nothing was submitted. Row hit areas measured at least 58 pixels at 320-pixel width. This is UI fixture evidence, not proof of a saved live account transfer.
- Desktop at 1440 pixels retained one swing table, its Save changes editor, zero companion reports and no overflow. A viewport-resize check initially placed the last companion link beneath the tabs at desktop width; the intended iPhone tap followed by desktop inspection passed. Companion-at-desktop-width padding remains in the global responsive audit. Initial development Fast Refresh navigation interruptions were retried after terminal failure; no server restart was used to turn an observation timeout into success.
- Verification: 481 Vitest files / 2,309 tests passed, including preparation volume, recovery limits, legacy stage recovery, failed-transfer priority and surface-navigation coverage. TypeScript through the production build, changed-file ESLint/formatting and all unchanged route budgets passed. The shared navigation fix exposed a 75-byte Quick Bag overage; shorter supporting copy removed it without changing controls or evidence. No budget increased. QA servers are stopped and generated TypeScript configuration changes restored.
- Local ignored screenshots under `output/playwright`: `iphone-speed-stages.png`, `iphone-speed-build-stage.png`, `iphone-speed-max-fatigue.png`, `iphone-speed-session-review.png`, `iphone-speed-review-dark.png`, `iphone-speed-transfer-picker-fixture.png` and `desktop-speed-session-preserved.png`. Wider requirement-by-requirement acceptance remains open.

- PR #40 release review: CodeQL classified the helper name `mobileSpeedPrescription` as a sensitive-data source when its golf plan reached the existing local draft cache. Renamed it to `mobileSpeedPlanForTransfer` to describe the actual golf-only calculation. No scan suppression or data-storage behavior changed.

- PR #40 review follow-up: page cache names now derive from the asset-cache version, and failed navigation falls back to the current precached offline shell. An upgrade regression seeds the old HTML/cache, activates the new worker and verifies recovery of the new shell. Quick Bag caches only the compact calculation result for each authenticated owner for 60 seconds; authentication stays outside the cache, and existing shot mutations invalidate the Quick Bag path or root layout. A cache miss still reads the full evidence history to preserve existing confidence and touch calculations; this change removes that work from repeated route requests without truncating evidence.

## Progress story continuation after PR #40 — 5 September 2026

Previous goal turn: **progress**. PR #40 merged as `544e52cf`, and local main matched GitHub. This continuation is local; the unrelated nightly workflow is preserved.

- Progress now selects its companion branch before desktop-only diagnosis, weekly review and timeline queries. The existing composite progress score and desktop calculations remain unchanged. A dedicated phone story presents Performance, Scoring, Training and Goals.
- Direction uses the existing last-two-session comparisons for each club, requiring at least three clean shots in both sessions. Trust alone, overlapping first/latest baseline windows and missing metrics cannot produce an improvement claim. Mixed lateral changes stay mixed; carry changes remain neutral. Comparison sample counts and early/moderate confidence are explicit.
- The focused club comparison shows previous/latest actual values and links to club evidence. The next practice and exact latest range-session review remain accessible. Supporting score methodology is a native disclosure; no desktop chart or table is rendered on the phone.
- Recent scoring selects completed rounds of the same course/simulator context and hole count. Actual nine-hole scores are not replaced by the handicap model's 18-hole equivalents. Active and incomplete rounds cannot become results. Recorded penalties, three-putt holes or double-bogey holes supply a factual scoring focus when available, without inventing an underlying cause.
- Training shows distinct logged days and session count over 28 days, excludes future dates, shows the last logged date, and asks users to fill missing activity before interpreting a quiet recent record. Fitness, recent load and golf form reuse the existing model. A compact 90-day chart switches among three readable 44-pixel controls, exposes its numeric scale and provides seven exact daily values. Modelled golf form is explicitly distinct from physical readiness.
- Saved goals show current/target values, the existing progress calculation, next action, deadline and evidence-source disclosure. Saved values are labelled as not automatically verified against newly imported sessions. The real account has no saved goals; its empty state remains honest, and populated-goal browser coverage remains open.
- WebKit at 320 pixels verified score 78, an early 7-iron lateral signal of 6.8 to 18.1 yd from 3 and 4 clean shots, actual Aintree nine-hole score 39 (+3), four logged training days / twelve sessions in 28 days and last log 22 August. One heading, no tables, no horizontal overflow, all chart controls, exact-value disclosure, latest-session navigation and Back passed. No account records were submitted or changed.
- Node and WebKit disagreed on September's abbreviated name during hydration; deterministic chart date labels fixed the mismatch. Subsequent light/dark reloads showed no hydration mismatch. Dark disclosure links use the existing readable green tint. The final compact controls read Fitness / Load / Form without truncation.
- Desktop at 1440 pixels retained Performance / Goals / Load / Timeline, working tab selection, zero companion reports and no overflow. The full test suite caught Progress styles outside the mobile media boundary; they were moved inside the existing boundary before the successful rerun.
- Local ignored screenshots: `iphone-progress-story-light.png`, `iphone-progress-story-dark.png`, `iphone-progress-training-final.png` and `desktop-progress-preserved.png` in `output/playwright`. Screenshots show the browser-verified layout; numeric chart-scale labels were added in the final build after that capture.

- Final checks: 483 Vitest files / 2,316 tests, changed-file ESLint/formatting, TypeScript through the production build, the final production build, all unchanged route budgets and diff whitespace passed. Progress is 921 KiB against its existing 1,025-KiB budget. QA server is stopped; generated TypeScript configuration changes were restored. This continuation has not been committed or pushed.

## Settings and recovery release continuation — 5 September 2026

- Mobile Settings now uses the shared grouped-list family and large titles. All nine existing section deep links remain valid. Each drill-down has contextual top navigation back to Settings and a separate scroll-restoration key. Desktop shortcuts, theme and density remain mounted behind a phone disclosure so their existing form values are preserved.
- Reset remounts the form controls, restoring Radix switches/selects and discarding the theme preview as well as restoring text. Saving state uses React form pending status. The phone save bar clears the persistent tabs and uses the existing keyboard-open shell state. Physical iPhone keyboard behavior remains part of the wider device audit.
- Shared route and global error boundaries now consume the installed Next.js `retry` prop instead of the retired `unstable_retry` name. The authenticated fallback is consolidated with the existing segment fallback and offers a mobile route back to Today.
- Live WebKit at 320 pixels verified one Settings heading, nine index links, working section Back, closed desktop preferences, text/switch/theme reset and no horizontal overflow. The dirty bar bottom was 695 pixels against the tab bar top of 746 pixels. No settings or account records were submitted. Light Settings and dark Appearance captures are in the ignored `output/playwright` directory.
- A temporary local throwing route verified the actual Next.js error boundary: after removing the local failure condition, tapping Retry recovered the same route successfully. The temporary route and flag were removed before the production build. Global-root recovery is covered by the API/source checks; it was not separately forced in the browser.
- Release validation before build: all 483 Vitest files / 2,316 tests and full ESLint passed. The broader mobile acceptance ledger remains open; this release does not claim completion of the entire brief.

- Final release gate: production build and TypeScript passed; all unchanged route budgets passed after removing the heavyweight premium-component import from the shared error fallback. Quick Bag is 873 KiB against 903 KiB and Progress 866 KiB against 1,025 KiB. The recovery behavior is unchanged; focused error tests and lint passed after this dependency removal. Generated build-only TypeScript configuration was restored, and the unrelated nightly workflow remains byte-for-byte preserved.

## Mobile hole strategy continuation after PR #41 — 5 September 2026

Previous goal turn: **progress**. PR #41 merged as `69768e41`; local main matches origin/main apart from this continuation and the preserved unrelated nightly workflow.

- The phone caddie book now opens with a compact course/tee selector, one hole title, a mapped overhead view and an open decision panel. Carry is a readable number, target and rationale follow, and a disclosure holds the club sample, confidence, lateral range, leave and course caveats. Quick Bag is accessible beside Course Twin. Previous/Next form a 44-pixel contextual bar above the persistent tabs.
- Safe/Normal/Aggressive carry their own club identity, measured carry and lateral evidence. Switching an option no longer retains the original club’s dispersion. Options with fewer than five shots are unavailable on mobile. Missing lateral readings or estimated carry ranges cannot draw an ellipse labelled measured. No map produces an explicit unavailable state rather than invented course geometry. Original recommendation and workbench calculations remain intact.
- Mobile dispersion follows the local hole direction at the map scale without minimum visual radii that exaggerate its width. The suggested path is straight from the reference tee; target placement is labelled indicative. The course reference tee is not presented as a measured location for the selected tee set. Wind, live hazards and unmapped distances remain explicit limitations.
- Hole and option live in the URL; changing holes restores Normal while preserving course/tee identity. Direct and remembered course/tee selection use the existing authenticated selection flow, with accessible Course/Tee labels, pending/error behavior and a Done control. Shared and owned course predicates now apply both to the bounded picker and direct strategy lookups.
- Saved snapshots are checked against the selected tee as well as account/course, and storage failures are surfaced. Audit found no offline consumer for the existing caddie-book snapshots. Removed the unsupported available-without-signal claim; implementing an offline reader remains open.
- Real-account WebKit at 320 pixels: Normal Driver 209 yd; Safe 5W 179 yd with 177–185 yd measured range, 42 yd left / 35 yd right and 17 shots at moderate confidence. The selected club, ellipse and exact course/tee/option survived reload. Hole 2 survived reload and returned to Normal. These are the existing strategy stock calculations, not the separate Quick Bag carry window.
- Light/dark views had one heading, no horizontal overflow and no hydration mismatch after SVG serialization rounding. Mode buttons measured 90×44 pixels; Previous/Next 112×44. A fresh browser was needed to discard an older development stylesheet: server CSS was current while the earlier browser retained old rules. No dev server restart was used to treat a timeout as success.
- Remembered course/tee were checked with temporary local browser cookies, restored afterwards. Course-selection sheet locked body scrolling; its 44-pixel Done button closed it and released scrolling. No account record was submitted or changed. Desktop at 1440 pixels retained the three-zone workbench and hole navigator, with zero companion panels and no overflow.
- Full suite: 484 Vitest files / 2,320 tests passed. New map tests cover per-option spread, missing evidence, missing geometry and preserved workbench overlay behavior; data tests cover visibility conditions and estimated-range provenance. The source motion check now verifies immediate hole scrolling rather than requiring an obsolete smooth-scroll implementation.
- Local ignored captures under `output/playwright`: `iphone-strategy-light-final.png`, `iphone-strategy-dark-final.png`, `desktop-strategy-preserved.png`. The complete redesign is still unproven against the remaining acceptance ledger.

- Final verification: production build/TypeScript, changed-file ESLint/formatting, every unchanged route budget and diff whitespace passed. No budgets were increased. The development server is stopped, generated TypeScript configuration changes restored, and the unrelated nightly workflow preserved byte-for-byte. These strategy changes remain local and uncommitted.

## Saved caddie-book offline continuation — 5 September 2026

Previous goal turn: **progress**. The strategy redesign remains local on main after PR #41; the unrelated nightly workflow is preserved.

- Saving a caddie book now creates an account-bound version 2 snapshot containing the existing strategy evidence, selected hole/option, tee identity and vector course geometry. Remote aerial imagery is deliberately excluded so the mapped hole does not depend on another network request. The UI links directly to the saved reader using document navigation.
- The public offline shell lists this account’s saved books and reuses the online hole-decision component. Exact course/tee deep links and requested holes are respected. Hole/option changes persist without renewing the download timestamp. The saved date, stale-copy guidance, reference-tee limitation and missing aerial imagery are visible. The context bar moves to the safe bottom edge because this shell has no primary tabs.
- A cached Quick Bag opens directly from the book and returns to the same hole. It retains the existing Quick Bag carry/total calculations and snapshot rather than replacing them with strategy-stock values.
- Readers reject unknown versions, account/key mismatches, malformed metrics and geometry, duplicate holes and invalid dates. A corrupt book cannot hide another valid book. Version 1 carries remain readable when their cached club identity is unambiguous; absent maps, per-option dispersion and measured-range provenance are not invented.
- Storage/focus/visibility checks clear the open private offline view when the active account marker changes. Practice and caddie selection persistence guard the current account marker before writing. Existing sign-out/account-switch purging already includes caddie-book keys.
- WebKit at 320 pixels saved a real 18-hole book with 115 mapped features (121,159 bytes). The shared reader opened hole 2 with no remote image element. Hole 3 / Safe / 8i survived reload while the original saved timestamp remained unchanged. A real second-tab storage event cleared the open book after an account-marker change; the original local marker and book were restored. No account record was submitted or changed.
- Production build verification used the actual service worker in an isolated local origin. Its 38 cached entries included the public offline shell and static assets, with no private HTML. After the server was explicitly stopped, an uncached course-strategy navigation opened requested hole 4 from the saved book. The mapped geometry and personal ellipse rendered; hole 5 / Safe / 5W survived reload. Quick Bag Carry/Total showed ten cached clubs and returned to hole 5. An uncached network probe failed, confirming the connection was unavailable; the snapshot’s download date stayed unchanged.
- Light/dark cold-offline views had no horizontal overflow; the dark hole title was white. The only production-browser console error was the deliberately failed network probe. Local ignored captures: `iphone-saved-caddie-book.png`, `iphone-caddie-cold-offline.png`, and `iphone-caddie-cold-offline-dark.png` under `output/playwright`.
- Service-worker cache version advanced to v9. The upgrade regression now seeds both the previous asset cache and v8 page cache, verifies their removal, and verifies recovery of the newly cached shell. Full suite: 485 Vitest files / 2,325 tests passed. Production build/TypeScript and every unchanged route budget passed. No budgets were increased.
- The wider mobile acceptance ledger remains open. These changes and the preceding strategy continuation remain local and uncommitted.

## Mobile low-power Course Twin replay — 5 September 2026

Previous goal turn: **progress**. PR #42 merged as `06a018dd`; local main and origin/main were confirmed equal. This continuation is local work after that release, with the unrelated nightly workflow retained.

- The prior phone low-power fallback discarded replay data and presented an entire-course overview. It now opens a single mapped hole with Plan/Replay controls, Play as a handoff to the selected course hub, and contextual Club strategy/Quick Bag links. The desktop low-power overview remains unchanged. Shared read-only views omit personal scoring and bag actions.
- Replay consumes the existing account-scoped replay document. The selected shot shows its actual club, carry/total/side values when recorded, and session date. Course placement remains derived, flight/roll remain reconstructed, and the evidence disclosure distinguishes the lighter replay from terrain-aware 3D simulation. No new golf outcomes or historical plan comparisons are invented.
- The SVG map keeps metres isotropic, orients the reference tee below the green, uses supplied course features and includes wide shot endpoints in its bounds. Playback interpolates the supplied trajectory and only animates roll when the existing document marks it reconstructed. Animation is capped at 30 updates per second, pauses when hidden, and stops when reduced motion is enabled. Reduced motion offers static start/result and manual scrubbing.
- Hole and shot selection preserve course/session/mode in the URL. Refresh restores the chosen shot. Previous/Next and swipe change holes; Plan hides replay controls. The balanced-3D handoff retains the current URL context through document navigation. The initial runtime quality snapshot no longer mounts the 3D renderer before browser quality detection.
- Real-account WebKit at 320×812: Bootle, 7 May, hole 1 had two eligible shots; the first was 8i with 128 yd measured carry. Playback moved, Pause held, scrubbing reached the endpoint, and selecting the second shot (sw) survived reload. Hole 2 and Plan/Replay switching worked. Reduced-motion Show result reached 100%, and toggling motion preferences during playback stopped rather than resumed it. The shot selector and replay controls measured 44 pixels high; one H1 and no horizontal overflow were present. Dark-mode heading was white.
- Network inspection in the phone 2D route showed no Three.js, React Three Fiber, heightmap or GLB asset requests. A small dynamic-loader entry was present; it did not load its heavy dependencies. Physical-device energy usage and production-network measurements remain unverified.
- Aberystwyth exercised an actual no-replay state. At 1440 pixels, the desktop fallback retained all 18 hole rows, no mobile overhead component and no overflow. Component tests cover an empty selected hole, missing metrics, deep-linked shot evidence and shared read-only controls. Projection/interpolation tests cover scale, wide misses, endpoints and unavailable roll.
- Full verification: 487 Vitest files / 2,332 tests passed; changed-file ESLint, production build/TypeScript and all unchanged route budgets passed. Course Twin is 860 KiB against 1,025 KiB. No budgets were increased. Generated build-only TypeScript configuration was restored.
- Ignored local screenshots: `iphone-overhead-replay-light.png`, `iphone-overhead-replay-dark.png`, `desktop-overhead-fallback-preserved.png`. The development server was deliberately stopped before building; subsequent disconnected HMR messages are not application failures. No account records were changed.
- The complete mobile objective remains active against this ledger. This continuation is uncommitted and has not been pushed.

## Mobile shot explorer and review continuation — 5 September 2026

Previous goal turn: **progress**. The verified low-power replay implementation remains local after PR #42. This turn continues the original sections 17–19; it does not change their acceptance scope.

- Replaced the phone's always-visible filter form with compact search and a scrollable filter sheet with fixed Apply/Reset actions. Club, session, trust, shot type, review state, dates and order remain available. Search preserves existing date/sort/session context, and valid incoming filter values remain selected. Native selectors have 44-pixel heights and visible disclosure arrows. Desktop filtering is unchanged.
- Shot rows now show rounded carry with units, side, ball speed, launch and actual trust eligibility. The detail sheet suppresses every missing-value sentinel, labels yard/mph/degree/foot/rpm units, retains real zeroes and shows smash to two decimals from the stored value. Missing carry is explicit; absent review confidence is hidden. A top Done action stays accessible while the metric body scrolls. No calculations changed.
- Explorer and review batch selection are restricted to currently visible IDs, deduplicated and capped at 50. Page/filter context changes reset the selection. A refresh cannot submit removed or hidden rows. Review Keep retains selection on failure and reports a useful connection/retry message.
- Audit found that an imported suggested exclusion could show two Keep actions. Mobile queue rows now pass an explicit exclusion intent to the existing reversible review control. Each suggestion has distinct Keep, Exclude and Change club paths; ordinary desktop suggestion dismissal/restoration remains unchanged. Phone confirmation copy explains trusted-distance/recommendation effects without exposing internal compatibility-flag or audit-event terms.
- Real-account WebKit at 320×812: the list contained 2,018 stored shots and opened with an actual SW carry of 80 yd. Its detail showed recorded metrics with units and omitted unavailable spin. A later actual shot retained smash 1.43. The first row began at y=294, with no horizontal overflow.
- Combined 7i + trusted + full-swing filters returned the correct rows in descending carry order (169 through 147 yd on the first page). Selecting a shot and navigating to page 2 cleared the selection. Searching Augusta retained August date bounds, side order and trust. No-match search rendered the empty state. Filter actions stayed within the settled sheet, selectors measured 44 pixels, and Done/Cancel released body scroll locking.
- The live review batch contained 17 real suggestions. Browser interception blocked one Keep POST before it reached the server: the row and selection remained, with the new retry guidance. A second blocked probe verified the final copy. Exclusion and restoration explanations opened and were cancelled. Moving to an older review batch cleared selection. No personal account data was written. Existing domain/action tests cover successful owned mutations; this browser pass does not claim a live mutation round-trip.
- Dark phone views had no overflow. At 1440 pixels, the desktop shot table remained present with no mobile explorer and no overflow. The QA browser initially had no companion surface cookie and therefore showed the workbench; setting the explicit local companion preference corrected the test context without changing product routing.
- Full verification: 489 Vitest files / 2,337 tests passed; changed-file ESLint, production build/TypeScript and all unchanged route budgets passed. Shots is 1,005 KiB against 1,123 KiB; review is 911 KiB against 1,123 KiB. No budgets were increased.
- Ignored captures: `iphone-shots-list-polish.png`, `iphone-shots-dark-polish.png`, `iphone-shot-detail-polish.png`, `iphone-shot-filters-polish.png`, `iphone-shot-review-dark.png`. The development-only error probe was intercepted; no account records were changed.
- The complete mobile objective remains active. This pass and the preceding low-power replay remain local and uncommitted; the unrelated nightly workflow is preserved.

## Mobile Bag evidence and club-to-practice continuation — 5 September 2026

Previous goal turn: **progress**. PR #43 merged as `b89436c9`; local main and origin/main were verified equal. The release included the shared low-power replay default fix and its regression. This continuation is local work after that release; the full objective remains active.

- The actual account has ten active club records and no duplicate active families. Source inspection found that the old mobile Bag loader merged club types, whereas Quick Bag and club detail selected equipment IDs. Its sample labels also came from a different stock window: the actual 7 iron displayed 13 shots on Bag and 20 on detail, despite showing the same 145 yd carry.
- The companion ladder now uses the same account-bound, equipment-specific evidence as Quick Bag and detail. Both lists sort by measured carry and preserve each equipment record. Duplicate family labels reveal the model; identical models receive stable record ordinals. No records are merged, deleted or rewritten. Regression fixtures cover two distinct models, identical unlabelled models, stable ordering, missing carry, unchanged IDs and separate distance links. The account itself has no duplicate-family example to verify visually.
- Trust summary and rail tone now refer to those same per-club records. The actual account reports eight high-confidence clubs and 82% average stock confidence, replacing the old nine/83% from the shorter merged selection. A club with no measured sample does not contribute a fabricated zero-confidence percentage. Normal mobile Bag no longer loads the desktop grouped evidence or creates/loads a social profile; benchmarks remain an explicit secondary load.
- Target inspection found that the old adapter called a rounded course number “trusted carry.” The existing stock calculation already returns both values. Target now receives the equipment-specific measured carry/range, the existing course play number and measured median side. No stock or target-ranking formula changed. WebKit verified the 150 yd target: 6i shows 152 yd trusted carry and 155 yd play number; the 7i alternative shows 145 yd carry, 150 yd play number and a 9 yd left median. Desktop Bag retains its existing course-number ladder and workbench calculations.
- Club-to-practice inspection found a restricted lookup that silently changed a 4 iron, numbered hybrid or approach wedge to 7 iron. Quick Range now uses the existing companion club formatter and includes the supplied club in its selector. Persisted numbered-club sessions remain recoverable; an active/paused session still takes precedence over a newly opened club link. Regression coverage includes 4i, 3h and aw.
- Real-account WebKit at 320×812 verified Bag → 7 iron detail → Quick Range: 145 yd carry, 20 trusted shots and the correct 7 Iron focus. An isolated local 4-iron activity survived refresh; a new 3-hybrid link retained the unfinished 4-iron session. No personal account records were written. Quick Range's native WebKit select ignored its previous minimum height; explicit height/appearance and a disclosure icon now measure 44 pixels.
- The first browser retained an older development stylesheet. A fresh WebKit context verified the final selector at 44 pixels with appearance none. Dark screenshots were captured after colour transitions settled; heading, Quick Bag action and active tab read white, with no horizontal overflow. Desktop at 1440 pixels retained its full Bag workbench and no mobile ladder. The default desktop view is a visual ladder, so this pass does not claim a rendered table check.
- Full tests: 490 Vitest files / 2,344 tests passed; changed-file ESLint passed. Final production build/TypeScript and every unchanged route budget passed. Bag is 1,046 KiB against 1,104 KiB; Quick Bag is 873 KiB against 903 KiB. No budgets were increased. Generated build-only TypeScript configuration was restored. Ignored captures: `iphone-bag-evidence-ladder.png`, `iphone-bag-evidence-dark.png`, `iphone-quick-range-club-picker.png`.
- The wider requirement-by-requirement mobile audit remains open, including populated goals, session/round detail coverage and broader Course Twin interactions. These changes are local and uncommitted. The unrelated nightly workflow remains intact.

## Session stories and round Insights continuation — 5 September 2026

Previous goal turn: **progress**. The verified Bag and Quick Range changes remain local after PR #43. This continuation addresses the original session-review and round-detail requirements without changing the objective. Main/origin parity was freshly confirmed; the unrelated nightly workflow is preserved.

- Mixed practice reviews previously exposed one automatically selected club's metrics. The new compact Club results selector exposes every imported club, including a clear state for clubs with no trusted readings. Each story uses only that club's trusted rows, preserves independent metric sample counts and omits missing measurements. Club selection lives in the URL, recovers on refresh and resets the carousel. The selected-club shot link carries both the real session ID and the Shots route's `club` filter; an all-session link remains available.
- Club-speed and smash details now label the number of readings based on estimated club speed using the existing estimate classifier. Stored values and averages are retained. The actual 22 August session has 37 imported / 33 trusted shots: its 7 iron has three trusted readings from four imports, 139 yd median carry and three estimated club-speed readings. The linked explorer shows those three trusted shots and the fourth user-excluded shot. Driver has two trusted shots and a five-metric story because dispersion lacks the required three side readings.
- A live chart check exposed an older quality-tag-only trust path: its Trusted view included the user-excluded 7 iron. The mobile session chart now uses the authoritative trusted IDs already selected by the session service, so its three-point pattern agrees with the metric story. All shots retains all four points. This is scoped to the mobile session composition; other callers of the older pattern helper still require auditing. Existing desktop chart calculations remain unchanged.
- The detailed chart mounts only after Show shot pattern and unmounts on Hide. It opens on the story's URL-selected club. A fresh WebKit context initially requested a 679-byte dynamic entry that did not define the chart; the chart implementation chunks arrived when opened. This is development-browser evidence, not a physical energy or production-network measurement.
- Where the existing comparison says a club is new or lacks a fair baseline, the mobile next action now asks for another measured baseline block instead of implying an identified control weakness. Its Quick Range handoff retains that baseline focus. Desktop report wording and composition remain unchanged.
- Real-account WebKit at 320×812 verified the Next metric control, 7-iron URL recovery, Driver switching/reset, exact shot filtering, chart open/hide, Trusted/All shots, one H1 and no horizontal overflow. The selector measures 44 pixels. Regression cases cover unavailable clubs, imported clubs without trusted readings, estimate provenance and exact shot-link scope. No personal account records were changed.
- The real Bootle 7 May round retained score 83 (+13), 21 putts, two penalties and its existing simulator estimate. Summary/Scorecard/Map/Insights links updated the URL; all 18 score rows remained available and the map loaded actual saved shots projected onto course geometry. Insights now leads with the existing Penalty avoidance recommendation, its two recorded penalties, scoring breakdown, best/costliest holes and fairway/green outcomes. Proof and notes/correction controls remain available behind closed disclosures; the desktop review is unchanged.
- Fresh dark-mode WebKit verified a white selector label and 44-pixel height. A shared mobile polish issue was corrected: text-primary accents now use the existing `--ios-link` tint rather than the darker filled-action green. Comparison evidence, carousel actions and View all shots read RGB 99/217/155 in dark mode; filled buttons retain their separate action surface. Desktop at 1440 pixels retained its report with no mobile story and no horizontal overflow. An earlier RSC fallback occurred during Fast Refresh; the final fresh browser recorded no console errors before deliberate shutdown.
- Ignored screenshots: `iphone-session-club-story.png`, `iphone-session-club-story-dark.png`, `iphone-round-insights.png`. Final verification: 491 Vitest files / 2,348 tests, changed-file ESLint, production build/TypeScript and all unchanged route budgets passed. Session detail is 880 KiB against 913 KiB; round detail is 901 KiB against 957 KiB. No budgets were increased. Generated build-only TypeScript configuration was restored. The full objective remains active; these changes and the preceding Bag continuation are local and uncommitted.

## Publish review correction — 5 September 2026

- PR #44 review found that switching away from a linked 4 iron, numbered hybrid or approach wedge removed that linked club from Quick Range options. The formatted initial club now remains in the option set independently of the current selection. Three rendered-state regressions select Driver and verify that the linked club remains available.
- Pre-publish checks passed 491 files / 2,348 tests, full ESLint, production build/TypeScript and all unchanged route budgets. The selector correction passed all 11 focused tests, changed-file ESLint and TypeScript; GitHub checks must pass on the corrected commit before merge. The unrelated nightly workflow remains excluded.

## Mobile evidence consistency and import completion — 5 September 2026

Previous goal turn: **progress**. PR #44 merged as `6c849457`; local main and origin/main were verified equal. This continuation remains local after that release. The full mobile objective is still active.

- Audited every production `buildShotPatternPoints` caller. Today and companion import results were still deriving trust from old quality flags. Session review had a local ID override; recent history filtered review eligibility but could then reject a restored shot because its original quality flag remained. Quick Bag's remaining direct helper call belongs to the desktop path, already applies eligibility and handles restored flags; the mobile route uses its dedicated evidence service.
- The shared point builder now accepts an optional authoritative set of trusted IDs. Today, import results and mobile session review pass their existing service selections. Recent history passes the exact IDs selected by its existing review predicate. This removes the contradictory second trust decision for restored history. Callers without an authoritative set retain their existing behavior. No raw point is deleted, no metric is changed, and the dispersion/confidence formulas are unchanged.
- Regression cases cover explicit exclusion, suggested exclusion, warm-up, calibration, sensor errors, restoration with a retained quality/integrity flag, and an empty authoritative selection. The fixtures verify preserved raw IDs/measurements and trusted-only sample/median calculations. Restored-account mutations were not performed; restoration is covered by deterministic tests.
- Mobile import completion was still using a repeated heading, metric grids, several cards and a desktop audit table. It now leads with one saved-session/round title, the existing measured verdict and one primary review action. Flagged shots retain first priority. Real club names replace abbreviated comparison labels; absent comparisons ask for another baseline. Round imports lead with the round handoff and keep measured club comparisons in the evidence disclosure.
- Existing plan-versus-actual values remain available. The shared session-pattern disclosure mounts the chart only after opening it. Import details use a vertical definition list and retain every former audit field, triage count and session average; mixed-club averages are explicitly labelled. The imported-shot link opens the scoped mobile explorer. The mobile compact title now says Import result. Desktop receipt composition is unchanged.
- Read-only WebKit at 320×812: the real 22 August receipt saved 37 shots across nine clubs, with 33 stock-quality and four confirmed exclusions. Its 7-iron Trusted view contained three measured landing points; All shots retained four. Today correctly used seven trusted 7-iron readings from the full practice day. The import chart starts unmounted; opening and closing it works. The settled audit disclosure retained 111 raw rows, 16 unknown rows, four exclusions, source/parser/mapping, and all four existing averages without horizontal overflow. Initial reads during the disclosure animation were incomplete; the settled 1,305-pixel content height and full text were subsequently verified.
- The real Bootle 7 May round receipt retained 58 shots and links directly to its round review. It shows one Round saved title, no practice-plan action and no chart. Session history still listed all 24 recent records. At 1440 pixels, the desktop import workflow receipt remained present, with zero mobile result components and no overflow. Final round/history navigation recorded no page errors. One development navigation was interrupted by Fast Refresh during an edit, then succeeded when retried; there were stylesheet preload warnings, not a clean-console claim for the whole development run.
- Light/dark phone captures were inspected. The dark heading was white; primary/secondary links measured 44 pixels and the grouped next-plan row 56 pixels. One H1 and no horizontal overflow were verified. Ignored screenshots: `iphone-import-result-polish.png` and `iphone-import-result-dark.png`. No account records were changed; the isolated browser closed and the development server was deliberately stopped before production validation.
- Final verification passed 492 Vitest files / 2,356 tests, changed-file ESLint, production build/TypeScript and every unchanged route budget. Companion import result is 877 KiB against 908 KiB; session detail is 880 KiB against 913 KiB. No budgets were increased. Generated build-only TypeScript configuration was restored. The changes remain local and uncommitted; the unrelated nightly workflow is preserved.

## Saved activities, evidence scope and missing-session recovery — 5 September 2026

Previous goal turn: **progress**. The import/evidence continuation remains local after PR #44; this pass preserves that work and the unrelated nightly workflow.

- The review route treated joined shot rows as proof that a session existed. An owned record without mapped shots could therefore return 404. The companion now reads account-scoped session metadata and a count of owned stored shots independently. Malformed IDs return no result before querying UUID columns; missing/unowned rows stay unavailable. The count join and session predicate both constrain the owner.
- An explicit session request could also fall back to other uploads on its date when it had no joined shots. The existing Today data service now keeps that explicit ID even when no measurements match it. Regression tests place a measured neighbouring upload on the same day and verify empty raw/trusted arrays, no comparisons and no fabricated carry; the intentional full-day Today scope still includes that upload. Distance and comparison formulas are unchanged.
- An empty saved practice opens a companion activity review with its real date, location, notes, equipment notes and linked plan when available. It shows no performance score or confidence percentage. Import retains the linked practice-plan context. If stored shots exist but cannot enter the review, the primary action instead opens those exact imported shots for mapping/evidence checks. Absent notes and plan sections are omitted.
- Companion session links for known round types now redirect through the existing round review route helper. A read-only account query found two zero-shot real rounds and no zero-shot practice sessions. Real WebKit verified the Mountain Park record via its old `/sessions/75a35029-7516-4355-9dcf-c198f99d908c` link: it reached the round review with 43 (+8), 15 putts, zero penalties and its existing scorecard-only estimate. No round or measurement was created or changed.
- The real 37-shot practice review still showed the nine-club story and its six trusted Pitching Wedge readings from seven imports. The desktop route still rendered its existing MIXED heading with no mobile empty component or horizontal overflow. The attempted `data-session-review-report` selector returned zero because that marker is not present; this is not evidence of a full desktop report/table audit.
- Missing and malformed session URLs initially fell through to generic dashboard recovery. A session-specific not-found boundary now gives companion users one Session unavailable title and Open golf history action; workbench retains the existing recovery. WebKit verified the 44-pixel action, one H1, no overflow, white dark-mode heading and successful navigation back to Your golf history. The security-oriented checks use the same unavailable response for missing/unowned records.
- Because the account has no empty practice record, its new composition was rendered from an isolated regression fixture into the existing local phone shell. The captured fixture had saved notes and a linked plan; it showed no horizontal overflow and links of at least 44 pixels. This is component visual evidence, not a live empty-account round-trip. No fixture was inserted into the database. The capture is explicitly named `iphone-unmeasured-session-fixture.png`; actual recovery is `iphone-session-unavailable-dark.png`, both ignored under output/playwright.
- An initial component test caught text claiming notes were saved even when no notes existed; the final copy refers only to the recorded activity. Initial round readback saw the streaming loading state, so the redirect was rechecked after URL and heading completion. Expected missing-route responses and Fast Refresh occurred during QA; no clean-console claim is made for the complete development run. The browser closed and the server was deliberately stopped before validation.
- Final validation passed 496 Vitest files / 2,366 tests, changed-file ESLint, production build/TypeScript and all unchanged route budgets. Sessions is 904 KiB against 938 KiB; session detail is 891 KiB against 913 KiB. Generated build-only TypeScript changes were restored. This continuation and the earlier import/evidence changes remain local and uncommitted; the full redesign objective remains active.

## Mobile round map and release gate — 5 September 2026

- The mobile Map tab now shows one saved hole at a time, a lightweight course-centre-line projection, previous/next and direct hole selection, grouped shot rows, and bottom sheets for measurements and Carry/Total/background choices. Selection survives reload through the URL. Satellite imagery and the existing Leaflet viewer load only when requested. Desktop retains its full map, hole controls, chart explanation and export tools.
- Shared projection functions were extracted without changing their distance, lateral or remaining-yardage calculations. The mobile viewport preserves metre scale, fits recorded misses, and rounds only final SVG pixel coordinates to avoid Node/WebKit trigonometric hydration differences. Carry/Total fallbacks name the actual available measurement; scorecard estimates and absent measurements remain explicit.
- Real-account WebKit at 320×812 verified Bootle hole selection/reload, recorded 8-iron details (128 yd carry, 131.3 yd total, 9 yd right), sheet open/close, and on-demand satellite loading. The final fresh context recorded no console errors, one heading and no horizontal overflow. Satellite loaded six tiles, retained its 346×408 SVG overlay and had 44-pixel zoom controls. An older development context retained the previous stylesheet; final verification used a fresh context.
- Browser checking caught overlapping enlarged marker targets; their hit areas now sit behind all visible markers so nearby shot centres remain tappable. Dark-mode inspection led to using the existing filled-action colour and paired foreground for selected markers. The desktop check at 1440 pixels retained the specialist map, all 18 hole choices, Carry/Total, course/satellite, explanation and export controls, with no mobile map or overflow.
- Release checks passed all 498 test files / 2,373 tests and full ESLint. The final map corrections additionally passed all 14 focused regressions and changed-file lint/format checks. Production build and bundle-budget verification are recorded below when complete. The unrelated nightly workflow remains excluded. This release does not mark the full mobile redesign complete; remaining route, Course Twin, accessibility and offline acceptance work remains open.
- Final production build/TypeScript and every unchanged route budget passed. Round detail is 919 KiB against 957 KiB; session detail is 891 KiB against 913 KiB; companion import result is 877 KiB against 908 KiB. No budgets were raised. Build-generated TypeScript configuration was restored before staging.

### PR #45 review corrections

- Satellite now receives the URL-backed selected shot and propagates marker taps to the same measurement sheet and URL state as Course view. Both views receive the same plottable shot selection; rows with absent distances remain in the list, while a saved remaining distance can locate a Total marker only. Controlled selection no longer silently highlights another shot when the selected row has no plotted position.
- Added regressions for a non-first satellite selection, marker-to-URL propagation, completely absent distances, and remaining-distance-only placement. All 498 files / 2,375 tests passed, with 16 focused map tests and changed-file ESLint/TypeScript passing. WebKit verified a saved Sand Wedge selection switching to 8 Iron through a satellite marker: the URL changed to that recorded shot and its measurement sheet opened. Compact shot paths are non-interactive to prevent a subsequent path from intercepting endpoint taps; desktop path interactions remain available.
